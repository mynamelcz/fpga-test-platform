# 需求与产品定位

## 一句话定位

**通用 FPGA 芯片测试与流片原型验证一体化平台，是一套贯穿流片前 FPGA 原型验证、流片后样片 bring-up、研发功能验证、自动化回归、小批量筛选和客户现场复现的研发级验证平台。**

它的核心不是“做一块 FPGA 板”，而是把芯片测试和验证流程平台化：

```text
统一硬件底座
统一资源抽象
统一测试脚本
统一数据采集
统一报告输出
统一问题复现
```

## 背景问题

芯片研发和测试过程中，常见工具链通常是割裂的：

- 流片前用 FPGA 原型板、开发板、单片机对拖、临时脚本验证逻辑
- 流片后用临时测试板、示波器、逻辑分析仪、串口工具、Python 脚本 bring-up
- 小批量筛选再做夹具、半自动工装或借用外部测试资源
- 客户现场问题依赖人工复现、截图、日志和经验判断

这些方法都能解决单点问题，但难以形成长期复用的平台能力。

典型结果是：

```text
测试板重复做
脚本重复写
问题重复查
数据难沉淀
经验难传承
验证难闭环
```

## 目标用户

### 1. 芯片研发团队

适合正在开发数字芯片、MCU、接口芯片、混合信号芯片的研发团队。

需求：

- 流片后快速 bring-up
- 自动执行寄存器、接口、GPIO、时钟、复位、Boot 等基础测试
- 快速复现芯片异常状态
- 对每版 RTL / 固件 / 样片做回归
- 形成规范测试报告

平台价值：

- 降低每个芯片项目重新搭测试环境的成本
- 让研发早期就有可复用自动化验证工具
- 把问题现场从“人工描述”变成“可回放数据”

### 2. FPGA 原型验证团队

适合已有 FPGA 原型系统，或使用 FPGA 进行流片前功能验证的团队。

需求：

- 模拟芯片外部环境
- 进行协议对拖
- 做异常输入、边界时序、毛刺、超时测试
- 捕获关键状态和波形
- 做长时间自动化回归

平台价值：

- 替代或增强传统单片机对拖测试
- 提前发现流片后才会暴露的问题
- 让流片前验证数据能延续到流片后测试

### 3. 芯片实验室测试团队

适合负责样品验证、问题定位、版本回归和实验室自动化的测试团队。

需求：

- 标准化测试流程
- 自动判定 PASS/FAIL
- 保存测试日志、波形、寄存器 dump
- 支持不同 DUT 子板快速切换
- 与示波器、逻辑分析仪、电源、温箱等仪器配合

平台价值：

- 把实验室测试流程从“人肉操作”变成“脚本执行”
- 减少测试遗漏
- 提高问题复现效率

### 4. 小批量样品筛选团队

适合工程样片、小批量试产、客户样品出货前筛选。

需求：

- 一键执行固定测试流程
- 支持 Socket / 压接 / 探针夹具
- PASS/FAIL 快速判定
- 记录每颗芯片测试结果
- 生成批次报告

平台价值：

- 在不导入完整 ATE 的情况下完成研发级筛选
- 支持工程样品质量把关
- 为后续 CP/FT 或外部测试方案提供前期数据

## 核心痛点

### 1. 研发阶段测试手段分散

常见组合：

- FPGA 开发板
- 单片机板
- USB 转串口
- 示波器
- 逻辑分析仪
- Python 脚本
- Excel 表格
- 临时测试板

这些工具本身都没问题，但缺少统一调度、统一数据、统一报告和统一安全机制。

### 2. 普通 FPGA 开发板缺少测试平台能力

普通开发板通常缺少：

- DUT 可控电源
- 多电压域 IO 适配
- DUT 保护
- 可替换扩展板体系
- 自动化测试脚本引擎
- Pattern / Capture 数据模型
- 测试报告和数据沉淀
- 扩展板 ID 和安全互锁

因此普通 FPGA 板只能作为逻辑载体，很难直接成为测试平台。

### 3. 专业 ATE 成本高、导入重

ATE 强在量产一致性、并行测试、精密测量和效率，但对于早期研发：

- 成本高
- 测试开发周期长
- 导入门槛高
- 灵活性不如自研平台
- 不适合频繁变化的原型和实验性测试

本平台不替代 ATE，而是补足研发阶段和小批量阶段的自动化验证空白。

### 4. 协议正常测试和异常测试难以统一

传统 MCU/脚本适合做正常协议读写，但不擅长：

- 精确边沿控制
- 多 IO 同步 Pattern
- ns/us 级时序异常
- ACK/NACK 异常注入
- 时钟毛刺
- 协议半包、超时、乱序
- 失败前后窗口 Capture

FPGA 平台可以把“正常协议控制器”和“Pattern 异常注入”结合起来。

### 5. 模拟测试和 DUT 适配反复重做

MCU 和混合信号芯片常涉及 ADC、DAC、比较器、运放、LDO、参考源等模拟模块。

如果每颗 DUT 都重新做模拟测试板，会导致：

- 硬件重复设计
- 校准方法不统一
- 测试脚本不复用
- 数据格式不统一

平台应把高精度参数测试和研发功能验证分开：第一版先做研发级模拟功能验证，精密参数仍交给专业仪器或 ATE。

### 6. 流片前 FPGA 原型对拖验证需要升级

单片机对拖测试优点是简单、便宜、灵活，但在复杂芯片原型验证中存在局限：

- 并行 IO 少
- 时序精度有限
- 难做复杂 Pattern
- 难做高并发协议
- 难做长时间 Capture
- 异常注入能力弱

核心 FPGA 板可作为外部可控环境，提升原型验证能力。

## 产品定位

### 定位描述

本平台定位为：

> 面向芯片研发和验证阶段的通用 FPGA 自动化测试平台。

它处在以下工具之间：

| 工具 | 优势 | 不足 |
|---|---|---|
| 普通 FPGA 开发板 | 灵活、便宜、开发方便 | 缺少测试平台体系 |
| 单片机测试板 | 简单、易用、成本低 | 并行、时序、异常注入能力弱 |
| 示波器/逻辑分析仪 | 观测强 | 不负责自动化闭环 |
| 专业 ATE | 精度、效率、量产能力强 | 成本高、导入重 |
| 本平台 | 灵活、可复用、自动化、贯穿研发流程 | 不替代高端量产 ATE |

### 不是什么

本平台不是：

- 专业量产 ATE 替代品
- 高精度模拟参数测试仪
- 高速 SerDes 一致性测试系统
- 单纯 FPGA 开发板
- 单颗芯片定制测试板
- 只给某个项目临时使用的工装

### 是什么

本平台是：

- 研发级自动化测试底座
- 芯片 bring-up 工具
- FPGA 原型验证对拖平台
- 多项目复用的测试资源池
- DUT 功能验证和异常复现平台
- 小批量样品筛选辅助平台
- 数据化、AI 化验证流程的硬件入口

## 核心价值

### 1. 把研发测试流程标准化

把原来靠人工经验执行的流程变成配置和脚本：

```text
电源时序 → Reset → Boot → 寄存器检查 → Pattern 测试 → Capture → 报告
```

### 2. 同时支持正常通信和异常测试

平台同时包含：

- 协议控制器：用于正常读写、配置、状态查询
- Pattern 引擎：用于并行激励、异常时序、边界条件
- Capture 引擎：用于失败现场保存

### 3. 降低重复硬件开发

核心板长期复用，扩展板按场景复用，DUT 子板按封装快速迭代。

### 4. 提升可观测性

不仅能“发命令”，还要能看到：

- DUT 输出
- IO 波形
- 协议交易
- 电源电流
- Trigger 前后窗口
- 失败时的上下文

### 5. 支持自动化和批量执行

平台应支持：

- 一键测试
- 批量执行
- 回归测试
- 失败重跑
- 报告生成
- 测试数据入库

### 6. 贯穿芯片研发全流程

平台的长期价值在于贯穿始终：

```text
需求阶段：定义测试目标
设计阶段：生成验证计划
FPGA 原型阶段：协议对拖和异常注入
流片后：bring-up 和功能测试
版本迭代：自动回归
小批量：样品筛选
客户现场：问题复现
```

### 7. 符合 AI 时代的发展趋势

AI 可以基于文档、寄存器表、测试日志和 Capture 数据参与验证闭环：

- 生成测试计划
- 生成寄存器读写用例
- 分析失败原因
- 推荐补充测试
- 整理客户报告
- 对历史失败做归因

但 AI 必须通过平台 API 操作，不能绕过安全机制直接控制硬件。

## 适用场景

适合：

- 数字芯片功能测试
- MCU 外设功能验证
- 接口芯片协议测试
- 混合信号芯片研发级验证
- FPGA 原型对拖验证
- 样片 bring-up
- 回归测试
- 小批量工程样品筛选
- 客户现场问题复现

## 非主打场景

第一版不主打：

- 高精度模拟参数测试
- 高速 SerDes 眼图/一致性测试
- RF 测试
- 大规模量产 CP/FT 替代
- 多站并行高吞吐测试
- 全自动机械上下料
- 高温高压全套可靠性测试

这些场景可以预留接口，但不作为 MVP 核心目标。

## 产品边界

### 核心板负责

- FPGA 计算和时序执行
- Pattern 输出
- Capture 采样
- DDR 缓存
- 协议控制器
- Trigger / Sync
- 上行通信
- 扩展板接口
- 底层安全状态管理

### 扩展板负责

- 电平转换
- DUT 电源
- DUT 保护
- 模拟前端
- Socket / 子板连接
- 板卡 ID
- 场景专用接口

### 上位机软件负责

- 资源配置
- 测试脚本
- 流程编排
- 数据采集
- 报告生成
- 历史数据管理
- AI 接口

## 当前 DUT 规模假设

为了避免一开始设计过小，当前按较有余量的 DUT 规模规划。

### 封装规模

最大目标 DUT 封装按 **BGA256** 预估。

这不代表第一版必须完整支持所有 256 个球的高速/全功能测试，而是作为核心板和扩展接口的规划上限。

### 数字 IO 规模

建议：

```text
MVP 可用数字 IO：160~192 路
硬件规划预留：224~256 路可扩展 IO
```

原因：

- BGA256 中并非所有引脚都是数字 IO
- 需要扣除电源、地、模拟、时钟、调试、保留引脚
- 160~192 路可以覆盖大量 MCU/数字/接口类芯片研发测试
- 硬件预留 224~256 路，避免后续扩展时不够用

### 模拟规模

第一版模拟测试不追求高精度参数化，而是偏研发功能验证。

建议覆盖：

- 4~8 路 ADC 激励/采样相关资源
- 2~4 路 DAC 验证资源
- 若干比较器阈值输入
- 简化运放功能验证
- LDO/参考源基础电压和负载测试

## 第一版 MVP 目标

MVP 要证明三个闭环：

### 1. 数字测试闭环

```text
上位机配置 Pattern
  ↓
核心 FPGA 输出
  ↓
扩展板适配电平
  ↓
DUT 或回环响应
  ↓
Capture 采集
  ↓
自动判定 PASS/FAIL
```

### 2. MCU 模拟外设测试闭环

```text
DAC/电压源激励
  ↓
DUT ADC/比较器/运放响应
  ↓
数字接口读回结果
  ↓
上位机判定误差范围和功能状态
```

### 3. FPGA 原型验证闭环

```text
核心板模拟外部环境
  ↓
业务 FPGA 响应
  ↓
异常注入 / 边界测试
  ↓
关键状态 Capture
  ↓
自动回归报告
```

## 长期演进方向

长期可以演进到：

- 多核心板同步
- 更高 IO 数量
- 更复杂 Pattern 编译器
- 更强 Capture 分析
- 扩展板生态
- 客户项目模板库
- 测试数据仓库
- AI 辅助测试生成和问题分析
- 与外部仪器联动
- 半自动小批量筛选工装

## 成功标准

项目成功不只看硬件能不能跑，而要看是否形成平台能力：

- 新 DUT 接入是否更快
- 测试脚本是否能复用
- 问题是否能复现
- 数据是否能沉淀
- 报告是否能自动生成
- 流片前和流片后是否能共用一套验证思路
- 后续项目是否能少做重复硬件和重复脚本
