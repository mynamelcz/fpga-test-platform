# 通用FPGA芯片测试与流片原型验证一体化平台

## 项目定位

本项目定位为一套面向芯片研发、FPGA 原型验证、流片后 bring-up、研发验证、小批量筛选和客户现场复现的通用自动化验证平台。

它不是单纯的一块 FPGA 开发板，也不是要替代专业量产 ATE，而是一个可复用的研发级验证底座：

```text
流片前：FPGA 原型对拖验证 / 协议仿真 / 异常注入 / 回归测试
流片后：样片 bring-up / 功能验证 / 问题复现 / 小批量筛选
客户侧：现场问题复现 / 测试脚本交付 / 数据闭环
```

平台核心思想：

> 用一块长期复用的核心 FPGA 主板，加一组可替换扩展板和统一上位机软件，把分散的芯片测试、原型验证、自动化回归和问题复现串成一条完整链路。

## 一句话总结

**一套贯穿流片前 FPGA 原型验证到流片后芯片测试的通用 FPGA 自动化验证平台。**

## 为什么要做

当前芯片研发测试中常见问题：

- 每个项目都临时做测试板，硬件重复投入大
- 单片机对拖测试灵活但速度、并行度、异常注入能力不足
- 普通 FPGA 开发板能跑逻辑，但缺少电源、安全、扩展板、脚本、数据、报告体系
- 专业 ATE 能力强，但成本高、导入重，不适合早期研发和多项目快速迭代
- 流片前 FPGA 原型验证和流片后 bring-up 数据割裂，问题难复现
- 测试结果散落在示波器截图、Excel、脚本日志和个人经验里，难以形成长期资产

本平台要解决的不是“某一次测试”，而是把测试能力产品化、标准化、自动化、数据化。

## 项目目标

### 第一阶段目标

- 明确平台产品定位和边界
- 完成总体系统架构设计
- 完成核心 FPGA 主板资源规划
- 完成扩展板与 DUT 适配体系
- 完成 MCU 模拟外设测试方案
- 完成流片前 FPGA 原型验证方案
- 完成上位机软件架构
- 形成 MVP 实施计划和风险边界

### MVP 目标

第一版不追求覆盖所有芯片测试场景，而是先打通闭环：

```text
上位机配置
  ↓
核心 FPGA 执行 Pattern / 协议动作
  ↓
扩展板完成 DUT 电源、IO、电平、安全适配
  ↓
DUT 响应被采集
  ↓
上位机自动判定并生成报告
```

MVP 建议重点能力：

- 160~192 路可用数字 IO，硬件规划预留 224~256 路可扩展 IO
- 外挂 DDR，用于 Pattern 缓存、采样缓存、长向量回放和 Fail Capture
- SPI / I2C / UART / GPIO / PWM 等常用协议控制
- DUT 电源时序、限流、过压/过流保护
- 扩展板 ID 识别和配置文件加载
- 基础 Pattern 输出与 Capture
- FPGA 原型验证对拖 Demo
- MCU 简化模拟外设测试 Demo
- 自动生成测试日志和报告

## 当前设计结论

### 架构结论

平台采用四层结构：

```text
上位机软件层
  ↓
核心 FPGA 主板层
  ↓
扩展板 / 适配母板层
  ↓
DUT 子板 / 夹具 / 原型系统层
```

核心板提供长期复用的数字验证资源；扩展板承接不同测试场景；DUT 子板承接封装变化；上位机统一管理配置、脚本、数据和报告。

### 核心板结论

核心 FPGA 主板应优先保证：

- 足够数字 IO 余量
- DDR 缓存能力
- 稳定扩展接口
- 可控电源和安全保护
- Pattern / Capture / Trigger 基础能力
- 可升级固件架构

不建议把高精度模拟测试、高速 SerDes 一致性测试、复杂量产 ATE 能力都塞进核心板。

### 扩展板结论

扩展板不是简单转接板，而是承接场景能力的关键层。

至少规划三类扩展板：

1. 数字芯片测试适配板
2. MCU 简化模拟外设测试扩展板
3. FPGA 原型验证对接板

后续可增加小批量筛选夹具扩展板、客户现场复现板、特定接口扩展板。

### 产品战略结论

平台最大优势不是某一块板卡，而是“贯穿始终”：

```text
流片前验证
  → 流片后 bring-up
  → 研发功能验证
  → 回归测试
  → 小批量筛选
  → 客户现场复现
  → 数据沉淀和 AI 分析
```

这符合 AI 时代自动化、数据化、闭环验证的发展趋势。

## 文档导航

| 文档 | 内容 |
|---|---|
| [[01-需求与产品定位]] | 产品定位、目标用户、痛点、适用场景、MVP 边界 |
| [[02-总体系统架构]] | 平台分层、数据流、控制流、安全链路、资源抽象 |
| [[03-核心FPGA主板设计]] | FPGA、DDR、IO、时钟、电源、扩展接口、通信、可靠性 |
| [[04-扩展板与适配体系]] | 扩展板分层、接口规范、板卡类型、安全策略、配置文件 |
| [[05-MCU模拟外设测试方案]] | ADC/DAC/比较器/运放/LDO 等 MCU 模拟外设功能测试方案 |
| [[06-流片前FPGA原型验证方案]] | 核心板对拖业务 FPGA、协议仿真、异常注入、信号观测 |
| [[07-上位机软件设计]] | GUI、脚本、资源模型、数据管理、报告、AI 接口 |
| [[08-MVP实施计划]] | 分阶段实施路线、里程碑、交付物、验收标准 |
| [[09-风险与边界]] | 技术风险、产品边界、工程风险、缓解策略 |
| [[99-原始方案]] | 原始讨论方案归档 |

## 建议阅读顺序

1. 先看 [[01-需求与产品定位]]，明确为什么做、给谁用、边界在哪里。
2. 再看 [[02-总体系统架构]]，理解整体分层和数据闭环。
3. 然后看 [[03-核心FPGA主板设计]] 和 [[04-扩展板与适配体系]]，理解硬件怎么拆。
4. 针对两个重点方向分别看 [[05-MCU模拟外设测试方案]]、[[06-流片前FPGA原型验证方案]]。
5. 最后看 [[07-上位机软件设计]]、[[08-MVP实施计划]]、[[09-风险与边界]]。

## 下一步工作清单

- 冻结 MVP DUT 规模假设：BGA256、160~192 路可用数字 IO、预留 224~256 路扩展能力
- 初步选择核心 FPGA 档位和 IO Bank 规划
- 定义核心板到扩展板连接器数量、pinout、供电、安全信号
- 定义第一版扩展板：数字测试基础板、MCU 简化模拟外设板、FPGA 原型验证对接板
- 定义上位机配置文件格式：core_board / extension_board / dut / test_plan
- 制作最小闭环 Demo：Pattern 输出 → DUT/回环 → Capture → 判定 → 报告
