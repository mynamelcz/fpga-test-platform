# FPGA 选型与 IO 预算

## 章节定位

本章把 [[03-核心FPGA主板设计]] "待定义关键指标"表中悬空的核心项——**FPGA 型号、IO 数量与 Bank 分组、连接器 pinout、电压域规划**——从 TBD 推进为**可决策的选型对比与预算表**。

本章的输入来自两处硬约束：

- [[10-Pattern引擎时序规格]]：反向提出的时序相关必选项（OSERDES/IDELAY、线速率门槛、多时钟域）。
- [[01-需求与产品定位]] / [[03-核心FPGA主板设计]]：DUT 规模（BGA256）、IO 规模（160~192 用 / 224~256 预留）、DDR、上行通信。

> 本章定位为**选型框架与预算**，不是最终器件冻结。所有绝对数字均为目标值，最终须在 [[08-MVP实施计划]] 阶段 0/1 结合询价、交期、工具链评估后冻结。遵循 [[09-风险与边界]] 决策优先级：`安全性 > 架构复用性 > 可验证闭环 > 扩展余量 > 单点功能丰富度 > 成本极限优化`。

---

## 一、选型输入约束汇总（先把"必须满足"列清楚）

把散落在各文档的硬性要求收敛成一张筛选清单。**任何候选 FPGA 必须同时满足全部 P0 项**，否则平台核心能力会退化。

| 约束 | 要求 | 来源 | 等级 |
|---|---|---|---|
| 用户 IO 总数 | ≥ ~400（覆盖 256 DUT IO + 控制/触发/通信/调试 + DDR） | 本章第二节预算 | P0 |
| OSERDES / ISERDES | ≥ 8:1，SelectIO 线速率 ≥ 1.0 Gbps | [[10-Pattern引擎时序规格]] 模式 B | P0 |
| 可编程 IO 延迟（IDELAY/ODELAY） | 抽头 ≤ 150 ps | [[10-Pattern引擎时序规格]] 模式 C | P0 |
| DDR 控制器 | 支持外挂 DDR3/DDR4（硬核或成熟软核 IP） | [[03-核心FPGA主板设计]] | P0 |
| PLL/MMCM 数量 | 满足 fabric + fine(SERDES) + capture + DDR 多时钟域 | [[10-Pattern引擎时序规格]] | P0 |
| IO Bank 电压灵活性 | 多 Bank 独立供电，覆盖 1.2/1.8/2.5/3.3V | [[04-扩展板与适配体系]] | P0 |
| 逻辑资源 | 容纳 Pattern+Capture+4 协议+Trigger+DMA+通信，留 ≥30% 余量 | [[08-MVP实施计划]] 阶段 4 | P1 |
| BRAM/URAM | 无 DDR 时可跑短向量 + 浅 Capture | [[03-核心FPGA主板设计]] | P1 |
| 上行通信 PHY 接口 | 支持 USB3（FX3 类并行）或 GigE（RGMII/SGMII） | [[03-核心FPGA主板设计]] | P1 |
| 工具链成熟度 | 免费/低成本工具可覆盖该档位，社区/文档充分 | 工程效率 | P1 |
| 供货与交期稳定 | 关键料有备选，交期可控 | [[09-风险与边界]] 供应链风险 | P1 |
| 国产替代可行性 | 评估等价能力路径（见第六节） | [[03-核心FPGA主板设计]] | P2 |

> 关键提醒：**OSERDES + IDELAY 是第一道筛选门槛**。缺这两项，[[10-Pattern引擎时序规格]] 的模式 B/C 无法实现，平台退化为"只有同步向量的普通 FPGA 板"，核心卖点丢失。这条优先于成本。

---

## 二、IO 数量预算（反推 FPGA 规模）

从 DUT 规模与平台资源反推所需用户 IO 总数。

### 2.1 IO 用途分解

| 用途 | MVP 可用 | 预留规划 | 备注 |
|---|---:|---:|---|
| DUT 数字 IO（Pattern/Capture） | 160~192 | 224~256 | 单端 1:1 计 |
| 控制/管理（I2C/SPI/GPIO 给扩展板） | 16 | 16 | |
| Trigger / Clock / Sync | 12~16 | 16 | 含外部 in/out、多板同步预留 |
| 上行通信 PHY（USB3 并行 或 GigE RGMII/SGMII） | 24~32 | 32 | 取决于通信选型 |
| DDR3/4（x16 数据位宽估算） | ~50 | ~50 | 数据16+DQS+地址命令控制 |
| 调试（JTAG/UART/备用） | 8 | 8 | 部分走专用脚 |
| **合计（用户 IO 需求）** | **~270~314** | **~358~378** | |

### 2.2 结论：FPGA IO 档位

```text
预留场景用户 IO 需求 ≈ 360~380
  ↓ 加 SI 余量与 Bank 取整损耗
目标 FPGA 用户 IO ≥ 400（建议留到 ~450~500 更稳妥）
```

> 若高速子集采用差分（LVDS）做 SERDES 通道，相关通道 IO 占用翻倍——这会进一步推高 IO 需求，**选型时按"差分通道数 × 2"单独核算**，不要用单端口径一刀切。

### 2.3 ⚠️ 高时序通道数 vs SERDES/IDELAY 资源（不可只看 IO 总数）

IO 总数够，**不等于**每路都能跑 [[10-Pattern引擎时序规格]] 的模式 B（OSERDES/ISERDES + IDELAY）。真实约束在于：

- 每路高时序通道要同时占用 OSERDES + ISERDES + IDELAY/ODELAY；
- SERDES 与 IDELAY 数量、IDELAYCTRL 与时钟区域（clock region）划分**按 Bank 受限**，并非每个 IO 都能无条件铺满；
- 160~192 路**全部**启用模式 B，对中端 7 系（如 Artix-7）的时钟/SERDES 资源是很重的负担，很可能出现"IO 够、但 SERDES/时钟资源铺不满全通道"。

因此**高时序通道必须分档，不做全通道同精度承诺**：

| 通道档 | 能力 | 建议规模（MVP） | 资源占用 |
|---|---|---:|---|
| 高时序档 | 模式 B（1 ns 栅格、多边沿、异常注入） | ~64 路 | OSERDES+ISERDES+IDELAY 满配 |
| 同步档 | 模式 A（节拍同步向量） | 其余至 160~192 | 仅 fabric，省 SERDES |

> 修正提示：本章 3.x 把 Artix-7 200T 列为主候选，是按 **IO/成本** 维度；但若首批 DUT 需要 **大量** 高时序通道（远超 ~64 路），须重新核算 SERDES/IDELAY/时钟区域是否够用，并评估是否上 **Kintex 档**。选型冻结前必须做这笔"通道数 × 每通道时序资源"的账，不能只看 IO 总数。

---

## 三、FPGA 候选档位对比

下面给出**厂商中立的档位框架** + **具体可对标系列**（便于询价与工具评估），不预设结论。

### 3.1 档位框架

| 档位 | 用户 IO | SERDES/IDELAY | 适配度 | 风险 |
|---|---|---|---|---|
| 入门级（如 ~100k LUT 以下小封装） | 通常 < 250 | 多数有 OSERDES，IO 数不够 | ❌ IO 不足 | 很快不够用，淘汰 |
| **中端大封装** | **400~500** | **OSERDES 8:1 + IDELAY 齐全** | ✅ **MVP 主候选** | 成本/层数适中 |
| 高端（UltraScale/高速档） | 500+ | 强，但含大量 GT 收发器 | ⚠️ 过剩 | 成本、PCB、工具复杂度高 |

### 3.2 可对标系列（供询价/评估，非最终选型）

| 系列（举例） | 用户 IO（大封装） | OSERDES | IDELAY 抽头 | DDR | 工具链 | 备注 |
|---|---|---|---|---|---|---|
| Xilinx Artix-7（如 200T / FFG1156） | ~500 | OSERDESE2 8:1，LVDS ~1.25 Gbps | IDELAYE2 ~78 ps | DDR3 via MIG | Vivado（含免费档） | **MVP 性价比主候选** |
| Xilinx Kintex-7 | 400+ | 更高速 | ~78 ps | DDR3 | Vivado | 资源/速度更足，成本更高 |
| Intel Cyclone V / Arria | 中高 | 类似 OSERDES + 延迟链 | 有 | DDR3 硬核 | Quartus | 备选生态 |
| 国产（Pango/Gowin/Anlogic 等大封装档） | 视型号 | **须逐一核实 SERDES 速率与延迟抽头** | 须核实 | 视型号 | 各自工具 | 见第六节，作为并行替代轨 |

> Artix-7 200T（FFG1156，约 500 用户 IO、OSERDESE2、IDELAYE2 ~78 ps、DDR3 经 MIG、Vivado 工具成熟）是当前**最契合 P0 约束且成本/层数适中的 MVP 主候选**。但这是建议起点，不是结论——需用第一节清单逐项打勾后冻结。

---

## 四、IO Bank 分配表（目标）

承接 [[03-核心FPGA主板设计]] 的 Bank 用途分组，给出更具体的目标分配（以 ~500 IO、Bank 含约 50 IO 的中端器件为模型）。

| Bank 组 | 用途 | 通道目标 | 电压域 | 时序要求 |
|---|---|---:|---|---|
| DUT_BANK_A | DUT 数字 IO 组 1（Pattern/Capture） | 32 | 可配（默认 1.8/3.3） | 高，SERDES/IDELAY |
| DUT_BANK_B | DUT 数字 IO 组 2 | 32 | 可配 | 高 |
| DUT_BANK_C | DUT 数字 IO 组 3 | 32 | 可配 | 高 |
| DUT_BANK_D | DUT 数字 IO 组 4 | 32 | 可配 | 高 |
| DUT_BANK_E | DUT 扩展/协议/备用 | 32 | 可配 | 中 |
| DUT_BANK_F | DUT 扩展/协议/备用 | 32 | 可配 | 中 |
| (预留 G/H) | 扩展到 224~256 路 | 64 | 可配 | 中 |
| CTRL_BANK | 管理 I2C/SPI/GPIO、扩展板 ID | 16 | 3.3 固定 | 低 |
| TRIG_CLK_BANK | Trigger/Sync/外部时钟 | 16 | 2.5/3.3 | 高（布线/抖动敏感） |
| COMM_BANK | USB3/GigE PHY 接口 | 32 | 1.8/2.5 | 高 |
| DDR_BANK | 外挂 DDR | ~50 | 1.35/1.5 | 高（等长/阻抗） |
| DEBUG_BANK | JTAG/UART/调试 | 8 | 3.3 | 低 |

设计约束（写入 PCB 约束文档）：

- 同一 Pattern 组的 DUT IO **尽量同 Bank 或相邻 Bank**，减少跨 Bank 时钟/skew 差异（呼应 [[10-Pattern引擎时序规格]] 第五节 skew 预算）。
- 同一 DUT 电压域引脚映射到同电压 Bank，避免跨域拉低整组电平。
- TRIG_CLK_BANK 与 DDR_BANK 走线敏感，优先分配到布线条件好的 Bank。

---

## 五、连接器选型与 Pinout 预算

承接 [[03-核心FPGA主板设计]] "建议接口分区（J1~J6）"，给出 pin 数预算。

### 5.1 信号→引脚预算

| 连接器 | 承载 | 信号 pin | GND/电源 pin | 合计估算 |
|---|---|---:|---:|---:|
| J1 | DUT IO 组 A/B（64 路） | 64 | ~64（1:1 GND，SI 考虑） | ~128 |
| J2 | DUT IO 组 C/D（64 路） | 64 | ~64 | ~128 |
| J3 | DUT 扩展 IO / 协议（64~128 路） | 64~128 | ~64 | ~128~192 |
| J4 | 电源与安全（12V/5V/3.3V、EN/PGOOD/FAULT/电流回读） | ~16 | 大电流多 pin | TBD（按电流） |
| J5 | Trigger/Clock/Sync | 12~16 | 配足 GND | ~32 |
| J6 | 调试/预留 | 8~16 | — | ~16~32 |

> SI 经验：高速/敏感数字组按 **1 GND : 1~2 signal** 配地；低速组可放宽到 1:4。最终比例随 [[10-Pattern引擎时序规格]] 端到端 skew 预算与实测调整。

### 5.2 连接器型号方向（候选）

| 类型 | 适合 | 说明 |
|---|---|---|
| 高密板对板（如 Samtec SEARAY / 类 FX 系列） | 核心板↔扩展板主力 | 密度高、机械可靠、成本中高 |
| FMC（LPC 160pin / HPC 400pin） | 若想兼容现成 FPGA 生态 | 标准化好，单连接器 IO 上限受限，可能需多个 |
| 排针/排母 | 调试/低速/预留 | 成本低、可靠性一般，不用于高速主通道 |

设计原则（来自 [[03-核心FPGA主板设计]]）：不把所有信号挤到一个连接器；按 Bank 分多连接器；电源大电流单独规划；关键控制信号防呆。

---

## 六、国产替代评估轨（并行,不阻塞 MVP）

[[03-核心FPGA主板设计]] 明确希望评估国产替代。本章给出**评估门槛**而非结论：

```text
国产 FPGA 入选必须逐项验证（对应第一节 P0）：
  1. 用户 IO 是否 ≥ 400（大封装档）
  2. 是否有 OSERDES 等价能力，线速率能否到 ≥ 1.0 Gbps   ← 最易卡住
  3. 是否有可编程 IO 延迟（IDELAY/ODELAY 等价），抽头粒度
  4. DDR3/4 控制器 IP 是否成熟
  5. PLL 数量与多时钟域能力
  6. 工具链综合/时序收敛是否可用
  7. 交期与长期供货
```

> 风险提示：国产器件最易卡在 **第 2、3 项（SERDES 线速率 + 延迟抽头）**。若达不到，则该器件只能跑 [[10-Pattern引擎时序规格]] 的**模式 A（同步向量）**，模式 B/C 不可用。可作为"低时序档替代方案"保留，但须在产品定位上明确标注能力差异，不可与主候选混为一谈。

---

## 七、风险与替代料

| 风险 | 影响 | 缓解 |
|---|---|---|
| 大封装 FPGA 交期长/涨价 | 阻塞打样 | 选型期锁定 ≥2 个可 pin/资源兼容候选 |
| 高密连接器采购周期长 | 阻塞打样 | 优先成熟型号，提前备货 |
| IO 实际可用数被特殊功能脚扣减 | IO 不足 | 预算已留余量，选 ≥400 而非刚好 |
| DDR 软核 MIG 占用逻辑/调试难 | 拖慢固件 | 分阶段启用 DDR（见 [[09-风险与边界]]） |
| 差分通道翻倍吃 IO | 超预算 | 差分通道数单独核算（见第二节） |

---

## 八、待实测/询价后冻结项

| 项 | 冻结条件 |
|---|---|
| FPGA 最终型号与封装 | 第一节清单逐项打勾 + 询价/交期确认 |
| IDELAY 实际抽头步进 | 选型后查 datasheet + bring-up 实测 |
| SelectIO 实际可达线速率 | 选型 + 板级 SI 仿真/实测 |
| DDR 容量/位宽/速率 | 结合带宽预算（建议新建 `13-带宽与成本预算`）|
| 连接器最终型号与 pinout 表 | 完成完整信号清单后定 |
| 各 Bank 最终电压域 | 结合首批扩展板 DUT 电压需求 |

---

## 当前结论

```text
· IO 预算反推：目标 FPGA 用户 IO ≥ 400（建议 ~450~500 留余量）
· 第一道门槛：OSERDES 8:1 @≥1.0 Gbps + IDELAY ≤150 ps（缺则核心卖点丢失）
· MVP 主候选：中端大封装（Artix-7 200T 类）满足 P0 且成本/工具适中
· 国产替代并行评估，门槛卡在 SERDES 线速率与延迟抽头
· Bank 按"DUT 组优先同域同区 + 敏感信号专 Bank"分配
· 连接器按 Bank 分 J1~J6，高速组 1:1~1:2 配地
```

本章把 [[03-核心FPGA主板设计]] 大半张 TBD 表推进到了可决策状态，并与 [[10-Pattern引擎时序规格]] 形成闭环（时序规格提约束 → 本章据此选型）。**建议本章与时序规格一起在 [[08-MVP实施计划]] 阶段 0 冻结**，因为它是后续电源树、PCB 层数、连接器、扩展板接口的共同上游。下一步自然衔接到 **Capture 引擎规格** 与 **带宽/成本预算**。
