# 扩展板与适配体系

## 章节定位

扩展板是平台从“通用 FPGA 核心板”走向具体芯片、具体封装、具体测试场景的关键层。

核心板保持稳定和长期复用，扩展板负责把不同 DUT 的封装、电源、模拟接口、保护、电平转换、夹具和行业场景适配出来。

本章节重点定义：

- 扩展板体系怎么分层
- 核心板和扩展板之间怎么接口
- 不同类型 DUT 怎么适配
- 哪些能力放核心板，哪些能力放扩展板
- 第一版 MVP 应该先做哪些扩展板

## 总体设计原则

### 1. 核心板不直接绑定 DUT

核心 FPGA 主板不直接做某一颗芯片的专用夹具，也不直接承载复杂模拟测试链路。

合理边界是：

```text
核心板：通用数字资源、Pattern、Capture、DDR、Trigger、上行通信、电源管理
扩展板：DUT 封装、电源生成、保护、模拟前端、收发器、夹具、场景适配
上位机：测试脚本、流程编排、数据分析、报告、AI 闭环
```

这样核心板可以长期复用，扩展板可以按芯片类型和项目快速迭代。

### 2. 扩展板不是简单转接板

扩展板不只是“把 FPGA IO 接到 DUT 引脚”的转接板，而是平台能力的重要组成部分。

扩展板至少要承担：

- DUT 封装适配
- 引脚映射
- 电平转换
- ESD/过压/反灌保护
- DUT 电源生成与监控
- 模拟/专用接口前端
- 扩展板身份识别
- 板级校准和版本管理
- 测试夹具、插座、探针、排线适配

### 3. 按场景做扩展板，不按单颗芯片孤立设计

扩展板体系要避免每颗芯片都从零画一块完全不同的板。

建议按“通用母板 + DUT 子板 / 封装小板”的方式沉淀：

```text
核心 FPGA 主板
  ↓
通用扩展母板
  ↓
DUT 封装子板 / 插座板 / 探针夹具
  ↓
待测芯片
```

这样可以复用电源、保护、模拟矩阵、连接器和管理逻辑，只替换低成本 DUT 子板。

## 扩展板分层结构

### 层级 1：核心板扩展接口

这是核心 FPGA 主板上的固定连接器，决定平台扩展上限。

应长期稳定，不随某一颗 DUT 频繁变化。

需要承载：

- 224~256 路规划数字 IO
- 管理总线 I2C/SPI
- 扩展板识别 EEPROM
- 12V/5V/3.3V 辅助电源
- 电源 EN/PGOOD/FAULT/INT
- Trigger In/Out
- Sync
- 外部时钟输入/输出
- GND 回流与屏蔽
- 机械定位和防呆

### 层级 2：通用扩展母板

通用扩展母板承接核心板资源，并提供常用测试功能模块。

可按场景拆成不同母板：

- 数字芯片测试母板
- MCU 模拟外设测试母板
- FPGA 原型验证对接母板
- 小批量筛选夹具母板

母板负责相对稳定的部分：

- IO 分组和缓冲
- 可选电平转换
- DUT 电源模块
- 保护电路
- 模拟矩阵或模拟前端
- 标准连接器
- 扩展板 ID
- 板温、电流、电压监控
- 插座/子板连接器

### 层级 3：DUT 子板 / 封装适配板

DUT 子板负责低成本、频繁变化的部分。

主要内容：

- QFN/QFP/SOP/BGA 等封装引脚扇出
- Socket 或焊盘
- 最后一层引脚映射
- 局部去耦
- 必要的 RC、跳线、电阻配置
- 测试点
- 批量测试夹具接口

对于研发阶段，DUT 子板可以是低成本 2~4 层板；对于高速或 BGA DUT，再根据 SI/PI 要求升级层数。

## 核心板与扩展板接口规范

### 数字 IO 分组

建议核心板到扩展板的数字 IO 按 16/32 路为一组。

推荐分组口径：

| 项目 | 建议 |
|---|---|
| 总规划数字 IO | 224~256 路 |
| MVP 可用数字 IO | 160~192 路 |
| 单组规模 | 16 或 32 路 |
| 分组依据 | FPGA Bank、电压域、协议组、DUT 端口 |
| 每组附带信号 | GND、Bank 电压标识、OE、DIR、状态、屏蔽/保护 |

设计注意：

- 不要把所有 IO 当成完全等价的普通 GPIO。
- 要预留时钟、Trigger、Reset、Mode、Interrupt 等特殊用途通道。
- 同一电压域的 DUT 引脚尽量映射到同一组或相邻 IO 组。
- 扩展板应把高速/敏感信号和普通低速信号分区布线。
- 对于未使用 IO，扩展板上要有默认安全态，避免悬空误触发。

### 管理接口

扩展板必须有身份识别和状态上报能力，否则后续多扩展板生态会混乱。

建议每块扩展板包含：

- EEPROM 或小 MCU，存放板卡 ID
- 扩展板类型
- 硬件版本
- 支持的 DUT 类型
- IO 映射表版本
- 电源能力
- 校准数据
- 生产序列号
- 关键器件信息

核心板上电后先读取扩展板信息，上位机据此自动加载对应配置模板。

建议流程：

```text
插入扩展板
  ↓
核心板管理 MCU 读取扩展板 EEPROM
  ↓
校验板卡类型、版本、电源能力、IO 映射
  ↓
上位机加载对应测试模板
  ↓
确认无冲突后允许扩展板和 DUT 上电
```

### 电源接口

核心板只向扩展板提供受控辅助电源，不直接承担所有 DUT 精密电源能力。

建议核心板提供：

- 12V_RAW 或 12V_SW
- 5V_AUX
- 3.3V_AUX
- 电源 EN
- PGOOD
- FAULT
- 电流检测回读
- 急停/故障关断

扩展板根据 DUT 生成具体电源：

- 1.8V
- 1.2V
- 1.1V
- 0.9V
- AVDD/DVDD/IOVDD
- 负压或特殊模拟电源
- 可编程电压
- 小电流精密参考

设计边界：

- 核心板负责“安全供电入口”和“故障保护”。
- 扩展板负责“DUT 所需具体电压”。
- DUT 电源默认不上电，必须在上位机确认配置和保护条件后再开启。
- 扩展板电源要支持电流监控，方便做短路保护、功耗曲线和异常分析。

### 时钟、同步与触发接口

扩展板接口需要保留统一的同步机制。

建议预留：

- Clock Output：核心板输出给 DUT 或扩展板
- Clock Input：外部参考或 DUT 回传时钟
- Trigger In：外部仪器或夹具触发平台
- Trigger Out：平台触发示波器、逻辑分析仪或其他设备
- Sync：多板同步或测试流程同步

第一版不追求专业时钟仪器能力，但要保证研发验证中常用的同步、触发、采集和复现能力。

## 扩展板类型设计

### 1. 数字芯片测试适配板

#### 目标

用于通用数字芯片、MCU 数字 IO、接口芯片、逻辑芯片、小规模 ASIC 等 DUT 的功能验证、bring-up、回归和小批量筛选。

#### 主要功能

- 封装适配：SOP/QFN/QFP/BGA/Socket
- IO 映射
- 电平转换
- 输入/输出方向控制
- ESD 保护
- 过压保护
- 防反灌
- 上拉/下拉配置
- DUT 电源生成
- Reset/Boot/Mode 控制
- DUT 电流检测
- 状态 LED 和测试点

#### 推荐结构

```text
核心 FPGA 主板
  ↓
数字测试母板
  ↓
封装适配子板 / Socket 板
  ↓
DUT
```

#### 设计重点

数字测试母板不要一开始追求覆盖所有封装，而是先把“IO 分组 + 电平 + 保护 + 电源 + 识别”标准化。

封装变化交给子板解决。

建议第一版数字测试母板支持：

- 160 路左右数字 IO 接入
- 3~4 组独立 DUT IO 电压域
- 每组独立 OE/方向/保护
- DUT 1~4 路可控电源
- DUT 电流检测
- 扩展板 EEPROM
- 常用接口引出：UART/I2C/SPI/GPIO/PWM
- 关键 IO 测试点

#### 适合场景

- 新芯片样品 bring-up
- MCU/ASIC 数字功能验证
- SPI/I2C/UART/GPIO/PWM 验证
- 寄存器读写验证
- Reset/Boot 模式验证
- Pattern 激励与响应比对
- 小批量样品筛选

### 2. MCU 模拟外设测试扩展板

#### 目标

用于验证 MCU 或混合信号芯片中的 ADC、DAC、比较器、PWM、定时器、GPIO、低速模拟接口等功能。

当前设计决策：

> 采用“可编程模拟矩阵母板 + MCU 专用子板”的结构。

#### 推荐结构

```text
核心 FPGA 主板
  ↓
MCU 模拟外设测试母板
  ↓
MCU 专用封装子板
  ↓
MCU DUT
```

#### 母板负责

- 可编程模拟开关矩阵
- 基准电压源
- 简单 DAC 激励
- ADC 采样
- 比较器输入激励
- PWM 捕获/测量
- GPIO 电平转换
- 电流检测
- 低速模拟量输出
- 保护和限流

#### 子板负责

- MCU 封装适配
- 引脚映射
- Boot/Reset/Debug 引脚
- 局部去耦
- 晶振/外部时钟选择
- 特定型号需要的跳线配置

#### 设计边界

第一版 MCU 模拟扩展板不定位为高精度仪器，不替代专业 DMM、示波器、源表或高精度 ADC 卡。

第一版重点是自动化功能验证：

- ADC 读数趋势是否正确
- DAC 输出是否在合理范围
- 比较器阈值是否动作
- PWM 频率/占空比是否正确
- GPIO 输入输出是否正常
- 低功耗/唤醒流程是否可自动测试

如果后续需要高精度参数测试，可以增加专用高精度模拟扩展板，而不是把复杂度塞进核心板。

### 3. FPGA 原型验证对接板

#### 目标

用于流片前 FPGA 原型验证阶段，把核心 FPGA 测试平台接入用户业务 FPGA 或原型板，实现协议交互、异常激励、信号观测和自动化回归。

#### 典型结构

```text
核心 FPGA 主板
  ↓
FPGA 原型验证对接板
  ↓
用户业务 FPGA / 原型系统
```

#### 主要功能

- 双 FPGA 互联
- 电平匹配
- 时钟同步
- Trigger 同步
- Reset/Mode 控制
- 协议外设仿真
- 异常激励注入
- 关键总线观测
- Pattern 回放
- Capture 采集

#### 设计重点

这类扩展板不一定直接接芯片裸 DUT，而是接用户已有 FPGA 原型板、评估板或接口板。

重点不是封装适配，而是：

- 接口标准化
- 线缆连接可靠性
- 信号完整性
- 时钟/复位/触发一致性
- 自动化回归脚本复用

#### 适合场景

- 流片前 FPGA 原型对拖验证
- 外设协议仿真
- 异常输入注入
- 长时间稳定性回归
- 流片后样品问题复现前的 FPGA 侧复现

### 4. 小批量筛选夹具扩展板

#### 目标

用于样品数量上来之后的研发小批量筛选，不是量产 ATE 替代品，但可以显著减少人工重复测试。

#### 主要功能

- Socket 或压接夹具
- DUT 快速插拔
- 条码/二维码记录预留
- 自动上电
- 自动下载/配置
- 自动执行测试脚本
- PASS/FAIL 指示
- 结果上传
- 简单分 Bin

#### 设计重点

小批量筛选夹具必须重视机械可靠性：

- 插座寿命
- 定位精度
- 防呆
- 接触电阻
- 压接一致性
- ESD
- 操作效率

这个方向适合作为平台后续商业化亮点，因为它直接解决研发、FAE、样品验证阶段的重复劳动。

> ⚠️ 吞吐定位说明：核心板是**单 DUT 串行**架构，不做多站并行（见 [[09-风险与边界]]）。因此本夹具定位为**低吞吐、研发级筛选**——价值在于把人工重复测试自动化，**不是产线 UPH 级筛选**，也不替代量产 ATE 的并行吞吐。对外措辞须用此口径，避免与"商业化亮点"叠加后抬高吞吐预期。若未来需要并行，走"多核心板同步"路线（[[01-需求与产品定位]] 长期演进），而非单板多站。
## 扩展板安全策略

### 上电前检查

上电前必须完成：

- 扩展板 ID 识别
- 扩展板版本校验
- IO 映射校验
- DUT 电源电压配置确认
- 电流限值配置
- IO 默认方向确认
- 未使用 IO 安全态设置
- 温度/故障状态检查

### 防呆与保护

建议加入：

- 机械防反插
- 连接器 Key 位
- TVS/ESD
- 串联限流电阻
- 电平转换芯片 OE 默认关闭
- DUT 电源默认关闭
- IO 默认高阻
- 过流自动关断
- FAULT 拉低硬件关断

### 异常处理

异常时优先保护 DUT 和核心板：

```text
检测到过流/过压/过温/扩展板 ID 不匹配
  ↓
立即关闭 DUT 电源
  ↓
关闭电平转换 OE
  ↓
FPGA IO 进入高阻或安全态
  ↓
上位机记录故障日志
  ↓
禁止继续执行测试脚本
```

## 扩展板配置文件

为了让上位机自动识别和调用扩展板，每块扩展板应配套一个配置文件。

建议包含：

```yaml
board_id: digital_adapter_v1
board_type: digital_test_adapter
hardware_version: 1.0
supported_dut:
  - qfn48
  - qfp64
io_groups:
  - name: bank_a
    channels: 32
    voltage: 3.3
  - name: bank_b
    channels: 32
    voltage: 1.8
power_channels:
  - name: dut_vdd
    range: 0.8-3.6V
    current_limit: 500mA
clock:
  supports_clock_out: true
  supports_clock_in: true
safety:
  default_io_state: hiz
  default_power_state: off
```

这部分后续会和上位机软件、测试脚本、AI 自动化生成测试用例关联起来。

## MVP 扩展板建议

第一版不建议同时做太多复杂扩展板，否则硬件、固件、上位机都会失控。

建议 MVP 先做三块：

### 1. 数字测试基础扩展板

优先级最高。

用于验证核心平台最关键能力：

- IO 输出
- IO 采样
- Pattern
- Capture
- 电平转换
- DUT 电源控制
- 扩展板识别
- 上位机脚本执行

### 2. MCU 简化模拟外设扩展板

用于验证平台是否能覆盖 MCU 常见外设自动化测试。

第一版可以先做低精度版本：

- 简单 DAC
- 简单 ADC
- 可编程电压
- PWM 捕获
- 比较器激励
- GPIO 验证

### 3. FPGA 原型验证对接板

用于验证平台“流片前也能用”的核心战略价值。

重点验证：

- 双 FPGA 对拖
- 协议仿真
- 异常注入
- Capture
- 自动化回归

## 扩展板版本管理

扩展板必须从第一版开始做版本管理，否则后续多块板、多批次板、多颗 DUT 混用时很容易出错。

建议每块扩展板至少具备：

- 板卡类型 ID
- 硬件版本号
- BOM 版本号
- 校准数据版本
- 支持的 IO 电压范围
- 支持的 DUT 电源范围
- 支持的模拟/协议资源列表
- 安全能力声明

这些信息可以存放在板载 EEPROM 或管理 MCU 中，由上位机启动时自动读取。

上位机不应只根据用户手动选择来判断扩展板类型，而应以板载 ID 为准，并和配置文件做交叉校验。

## 扩展板验收流程

每一类扩展板都应有独立验收流程。

### 通用验收项

- 板卡 ID 可读取
- 默认状态安全
- 电源开关受控
- 过流/过压保护有效
- IO 默认高阻
- 电平转换方向可控
- 与核心板连接稳定
- 配置文件可正确加载
- 报告中能记录扩展板型号和版本

### 数字扩展板验收

- 多通道 IO 回环通过
- Pattern 输出波形正确
- Capture 采样结果正确
- 方向切换无冲突
- 电压域切换符合预期

### 模拟扩展板验收

- DAC 输出校准通过
- ADC 采样校准通过
- 模拟开关通道映射正确
- 可控负载工作正常
- 保护电路不会影响正常测量

### FPGA 原型验证对接板验收

- Trigger/Clock/Reset 正常
- 对拖 IO 映射正确
- 电平转换稳定
- 异常注入可复现
- Capture 能关联测试用例

## 扩展板生态路线

扩展板体系建议分三步演进。

### 阶段 1：内部验证板

目标是跑通架构，不追求通用商品化。

重点：

- 数字测试基础板
- MCU 简化模拟外设板
- FPGA 原型验证对接板

### 阶段 2：项目复用板

根据真实项目沉淀可复用场景板。

可能包括：

- MCU 通用外设验证板
- 接口芯片协议验证板
- BGA/QFN DUT 母板
- 小批量筛选夹具板
- 客户现场问题复现板

### 阶段 3：标准化板卡生态

当平台能力稳定后，再考虑形成板卡标准。

重点：

- 统一机械尺寸
- 统一接口 pinout
- 统一板卡 ID
- 统一配置文件
- 统一安全认证
- 统一校准流程

## 待定义关键指标

| 项目 | 初步指标 | 备注 |
|---|---:|---|
| 扩展板接口总 IO | 224~256 路规划 | 与核心板保持一致 |
| MVP 实际接入 IO | 160~192 路 | 优先跑通系统闭环 |
| IO 分组 | 16/32 路一组 | 对应 Bank、电压域和协议组 |
| 扩展板电源输入 | 12V/5V/3.3V | 来自核心板受控输出 |
| DUT 电源 | TBD | 由扩展板按 DUT 生成 |
| 电源监控 | 必须有 | 电压、电流、PGOOD、FAULT |
| 扩展板识别 | EEPROM 或 MCU | 板卡 ID、版本、能力、校准数据 |
| 默认安全态 | IO 高阻、电源关闭 | 上电前禁止直接驱动 DUT |
| 连接器 | TBD | 需兼顾 IO 数量、GND、插拔寿命、成本、布线难度 |
| 机械防呆 | 必须考虑 | 防反插、防错板、防误上电 |
| 配置文件 | 必须有 | 后续上位机和 AI 测试生成依赖 |

## 当前结论

扩展板体系是这个平台能不能长期复用、能不能覆盖多类芯片、能不能商业化的关键。

核心设计结论：

```text
核心板做稳定通用底座。
扩展板做场景能力。
DUT 子板做封装变化。
上位机做配置、流程、数据和 AI 闭环。
```

第一版不要把扩展板做成“万能大杂烩”，而是先通过数字测试扩展板、MCU 简化模拟扩展板、FPGA 原型验证对接板三类板，把平台闭环跑通。

后续再按真实项目沉淀扩展板生态。
