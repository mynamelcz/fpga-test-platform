# 校准与基准夹具

## 章节定位

[[05-MCU模拟外设测试方案]]、[[10-Pattern引擎时序规格]]、[[12-Capture引擎时序规格]]、[[15-板间接口规范]] 都依赖"校准"，但各文档只说了**要校准**，没说**何时校准、用什么基准、谁发起、数据存哪**。本章把这条流程补齐——它是 deskew 与模拟测量误差管理能落地的前提。

> 纪律不变（[[09-风险与边界]]）：校准服务于"可验证闭环"与"测试一致性"，报告中必须**区分平台误差与 DUT 真实行为**（与 [[05-MCU模拟外设测试方案]]、[[10-Pattern引擎时序规格]] 一致），不可把平台未校准误差栽给 DUT。

---

## 一、两类校准

| 类型 | 对象 | 来源依赖 | 触发时机 |
|---|---|---|---|
| **数字时序去偏斜（deskew）** | Pattern drive / Capture strobe 通道间 skew | [[10-Pattern引擎时序规格]] 模式 C、[[12-Capture引擎时序规格]] | 换扩展板 / 换 DUT 子板 / 开机 |
| **模拟测量校准** | 板载 DAC 输出、ADC 增益偏移、参考源、负载电阻、开关误差 | [[05-MCU模拟外设测试方案]] | 换模拟扩展板 / 周期 / 温变 |

---

## 二、关键认知：校准随信号路径变，必须可重做

校准值绑定的是**整条信号路径**，不只是 FPGA。路径一变，旧校准即失效：

```text
信号路径 = FPGA → 核心板走线 → 连接器 → 扩展板走线 → 电平转换/模拟前端 → 子板 → DUT
            └────────── 换扩展板 / 换子板 / 换连接 都会改变这条路径 ──────────┘
```

因此：

- **换扩展板** → 该扩展板的 deskew 与模拟校准须重做或加载该板已存校准表。
- **换 DUT 子板** → 子板段路径变，高时序/高精度场景需补校准。
- **开机** → 校验校准数据有效性与时效，过期则提示重校。

---

## 三、校准触发时机（谁发起）

| 时机 | 发起方 | 动作 |
|---|---|---|
| 开机 / 连接核心板 | 上位机 | 读校准表，校验版本与时效；缺失/过期 → 告警 |
| 识别到新扩展板 board_id | 上位机（据 [[15-板间接口规范]] 第六节流程） | 加载该板校准表；无则要求执行校准 |
| 换 DUT 子板 | 用户/脚本 | 高时序/高精度路径触发补校准 |
| 周期 / 温度漂移 | 上位机策略 | 模拟校准重做（V1 仅记录温度、提示，不闭环补偿） |
| 用户手动 | 用户 | 强制重校 |

---

## 四、基准夹具（原方案从未提及，本章补）

校准需要"已知基准"，否则无从对齐。V1 定义两个轻量基准：

### 4.1 数字环回基准（deskew 用）

```text
· 一块简单环回夹具/扩展板：把核心板输出通道经已知等长路径回接到输入通道
· 用于测各通道边沿到达相对偏差 → 求解 IDELAY/ODELAY 补偿抽头
· 对应 [[03-核心FPGA主板设计]] bring-up 的"GPIO 回环测试"，可复用同一夹具
· 局限：环回基准覆盖到连接器；电平转换器之后仍是校准盲区（见 [[10-Pattern引擎时序规格]] 4.4）
```

### 4.2 模拟基准（模拟校准用）

```text
· 板载参考源 + 已知精度基准电阻网络作为一级基准（研发级）
· 更高要求时，用外部精密 DMM/源表做二级基准标定板载 DAC/ADC（预留接口，非 MVP 必需）
· 校准内容：板载 DAC 输出校准表、ADC 增益/偏移、参考源实测值、电阻网络实测、开关通道误差
  （与 [[05-MCU模拟外设测试方案]] 校准章节字段一致）
```

---

## 五、校准数据存储与版本绑定

```text
· deskew 校准表、模拟校准表存于扩展板 EEPROM（见 [[04-扩展板与适配体系]]/[[15-板间接口规范]]）
· 每份校准数据绑定：board_id、hardware_version、calibration_version、校准日期、校准温度
· 上位机加载时交叉校验（呼应 [[14-配置文件Schema规范]] calibration_version 字段）
· 校准数据与测试报告关联：报告须标注"本次使用的校准版本与日期"
```

---

## 六、校准流程

```text
开机/换板
  ↓
读扩展板 EEPROM：board_id + calibration_version + 日期 + 温度
  ↓
有效且未过期？─ 是 → 加载校准表，进入测试
  │
  否
  ↓
提示执行校准
  ↓
接基准夹具（数字环回 / 模拟基准）
  ↓
扫描并求解（deskew 抽头 / 模拟增益偏移）
  ↓
写回 EEPROM + 绑定版本/日期/温度
  ↓
进入测试，报告标注校准信息
```

---

## 七、V1 边界

### V1 做

- 开机/换板的校准有效性校验与加载
- 数字 deskew 静态校准（环回基准，校准后 FPGA→连接器段 < ±0.5 ns 目标）
- 模拟研发级校准（板载基准）
- 校准数据 EEPROM 存储 + 版本/日期/温度绑定
- 报告标注校准信息，区分平台误差与 DUT 行为

### V1 不做（推迟 V2+）

- 温度闭环动态补偿（V1 仅记录+提示）
- 电平转换器之后路径的自动校准（校准盲区，靠器件选型与等长约束控制）
- 二级精密标定自动化（预留外部仪器接口）
- 自动周期性后台重校

---

## 当前结论

```text
· 校准绑定整条信号路径，换扩展板/子板必须重做或加载对应校准表
· 数字 deskew + 模拟测量两类校准，各有触发时机与基准
· 基准夹具：数字环回（复用 bring-up 回环）+ 模拟板载基准（原方案缺，已补）
· 校准数据存扩展板 EEPROM，绑定版本/日期/温度，报告须标注
· 边界盲区（电平转换器之后）如实告知，不伪装成全链路精度
```

本章把分散在 [[05]][[10]][[12]][[15]] 的校准依赖收敛成一条可执行流程，补上了"基准夹具"这个原方案的空白。
