# 风险与边界

## 章节定位

本章用于明确项目风险、能力边界和缓解策略。

目标是避免两个问题：

- 需求无限扩张，最终做成不可控的大系统
- 边界说不清，导致用户误以为平台能替代所有测试设备

## 总体边界

本平台是研发级自动化验证平台，不是完整量产 ATE。

它擅长：

- 流片前 FPGA 原型验证
- 流片后样片 bring-up
- 数字功能测试
- MCU 外设研发级验证
- 协议测试和异常注入
- 回归测试
- 小批量工程样品筛选
- 客户现场问题复现

它不主打：

- 高精度模拟参数测试
- 高速 SerDes 一致性测试
- RF 测试
- 大规模量产 CP/FT 替代
- 多站并行高吞吐测试
- 全自动机械上下料

## 技术风险

### 1. FPGA IO 数量和封装复杂度风险

风险：

- BGA256 级 DUT 对 IO 数量和连接器提出较高要求
- FPGA 自身 BGA 扇出和 PCB 层数增加
- IO Bank 电压域规划复杂

缓解：

- MVP 可用 IO 定为 160~192 路
- 硬件预留 224~256 路扩展能力
- 按 Bank 分组连接扩展板
- 不承诺所有 256 pin 都作为高速可测 IO

### 2. DDR 和高速数据链路风险

风险：

- DDR 调试复杂
- Pattern/Capture 数据量大
- 上位机回读速度可能成为瓶颈

缓解：

- 第一版 DDR 功能先做顺序读写和 Buffer 管理
- Capture 支持触发窗口，避免无脑全量采集
- 上行通信优先选择 USB3 或 Ethernet
- 先实现基础带宽闭环，再优化性能

### 3. Pattern 引擎复杂度风险

风险：

- 如果一开始设计过复杂，会拖慢 MVP
- Pattern 格式和脚本语言容易变成大工程

缓解：

- 第一版只支持基础并行向量、循环、Trigger、比较
- Pattern 编译器先简单化
- 高级语法放到 V2

### 4. Capture 数据分析复杂度风险

风险：

- Capture 数据量大
- 波形显示、协议解码、失败定位都可能变复杂

缓解：

- 第一版先支持原始数据回读、CSV/VCD 导出、失败通道定位
- 协议解码先覆盖 SPI/I2C/UART 基础场景
- 高级可视化后续迭代

### 5. 电源和安全风险

风险：

- DUT 误上电导致芯片损坏
- IO 电压域不匹配导致反灌或烧毁
- 软件误操作绕过保护

缓解：

- DUT 电源默认关闭
- IO 默认高阻
- 电平转换 OE 默认关闭
- 扩展板未识别禁止上电
- DUT 配置未加载禁止驱动 IO
- 过流/过压/过温硬件关断
- 管理 MCU/CPLD 独立负责底层安全

### 6. 扩展板生态失控风险

风险：

- 每个 DUT 都做一块完全不同的扩展板
- 扩展板接口不统一，后续无法复用

缓解：

- 核心板接口标准化
- 扩展板分为场景板和 DUT 子板
- 扩展板必须有 ID 和配置文件
- 引脚映射通过配置管理

### 7. 模拟测试定位不清风险

风险：

- 用户可能期待高精度 ADC/DAC/LDO 参数测试
- 第一版硬件无法达到专业仪器水平

缓解：

- 明确第一版是研发级功能验证
- 报告中区分功能验证和参数测试
- 高精度测试预留外部仪器接口
- 不承诺替代源表、精密 DMM、ATE 模拟通道

### 8. 上位机软件膨胀风险

风险：

- GUI、脚本、数据、报告、AI、仪器控制全部做，容易失控

缓解：

- MVP 只做最小闭环
- 配置文件优先，复杂编辑器后置
- 报告先 Markdown/HTML
- AI 接口先预留，不做完全自主闭环

### 9. 流片前原型验证接口差异风险

风险：

- 不同业务 FPGA 板接口差异大
- 电压、连接器、时钟、Reset 不统一

缓解：

- 第一版做标准 Demo 对接板
- 用配置文件描述接口
- 不承诺兼容所有第三方 FPGA 板
- 高速接口和复杂板间同步放到后续

### 10. 产品定位被误解风险

风险：

- 被理解成普通 FPGA 开发板
- 或被理解成完整 ATE

缓解：

- 对外强调研发级验证平台
- 明确适用场景和非主打场景
- 用 Demo 展示“自动化闭环”和“数据沉淀”价值

## 产品边界

### 核心能力边界

平台核心能力：

- 数字 IO Pattern/Capture
- 常用协议控制
- DUT 电源和安全控制
- 扩展板适配
- 测试脚本执行
- 结果采集和报告
- 研发级模拟功能验证
- FPGA 原型对拖验证

### 不承诺能力

第一版不承诺：

- 任意高速接口完整测试
- 任意封装 DUT 直接兼容
- 精密模拟参数达到仪器级
- 量产吞吐能力
- 不需要人工调试即可全自动适配新芯片
- AI 完全自动生成可靠测试并直接执行

## 工程实施风险

### 硬件改版风险

第一版硬件大概率需要改版。

应对策略：

- 第一版定位为验证平台架构
- 关键接口多留测试点和飞线空间
- 扩展连接器预留冗余
- 电源和安全链路优先稳妥

### 供应链风险

风险：

- FPGA 价格和交期波动
- 高密连接器采购周期长
- DDR 和电源芯片替代困难

应对策略：

- 选型时关注供货稳定性
- 关键器件准备替代料
- 连接器优先选成熟型号
- BOM 标注风险等级

### 调试周期风险

风险：

- FPGA + DDR + 高 IO + 上位机联调可能拉长周期

应对策略：

- Bring-up 分层验证
- 先回环再接 DUT
- 先静态 IO 再 Pattern
- 先 BRAM Capture 再 DDR Capture
- 每个阶段形成测试记录

## 必须补充的内容

后续在进入硬件设计前必须补充：

- FPGA 具体型号和资源评估
- IO Bank 分配表
- 核心板扩展连接器 pinout
- 电源树和安全关断方案
- DDR 容量和带宽估算
- 上行通信方案选择
- 第一版扩展板原理框图
- 配置文件字段定义
- Pattern/Capture 数据格式
- MVP 验收用例

## 决策原则

遇到功能取舍时按以下优先级：

```text
安全性 > 架构复用性 > 可验证闭环 > 扩展余量 > 单点功能丰富度 > 成本极限优化
```

说明：

- 不能为了省成本牺牲安全和扩展性
- 不能为了功能多导致 MVP 无法闭环
- 不能为了快速演示做成一次性项目板
- 不能把第一版定位成量产 ATE

## 风险分级表

| 风险 | 影响 | 概率 | 优先级 | 策略 |
|---|---|---:|---:|---|
| IO 数量不足 | 高 | 中 | 高 | 预留 224~256 路扩展能力 |
| 电源误操作 | 高 | 中 | 高 | 硬件保护 + 安全校验 |
| DDR 调试复杂 | 中 | 中 | 中 | 分阶段启用 DDR |
| Pattern 设计过复杂 | 中 | 高 | 高 | MVP 简化格式 |
| 模拟精度预期过高 | 中 | 中 | 中 | 明确研发级边界 |
| 上位机功能膨胀 | 中 | 高 | 高 | 先做闭环，再做体验 |
| 扩展板不复用 | 中 | 中 | 中 | 标准接口 + 配置文件 |
| 供应链波动 | 中 | 中 | 中 | 备选器件 |
| 客户场景过散 | 中 | 高 | 中 | 聚焦三类 MVP Demo |

## 当前结论

这个项目可行，但必须守住边界。

第一版最重要的不是把所有测试能力都做完，而是证明：

```text
一块核心 FPGA 主板
一套扩展板体系
一个上位机软件
可以把流片前和流片后的验证流程串起来
```

只要 MVP 能完成数字测试、MCU 模拟外设功能验证、FPGA 原型对拖三类闭环，就已经具备继续投入和产品化迭代的价值。
