# 板间接口规范

## 章节定位

本章把核心板↔扩展板的接口，从 [[03-核心FPGA主板设计]]/[[04-扩展板与适配体系]] 的**分区级框架**和 [[11-FPGA选型与IO预算]] 的**pin 数预算**，推进为**逐针 pinout + 连接器冻结建议 + 电气/机械定义**的接口规格。

[[09-风险与边界]] 把"核心板扩展连接器 pinout"列为"进入硬件设计前必须补充"的头号项。本章就是来补它的。

> 接口是全平台**最该稳定、改动代价最高**的契约：连接器一旦定型，核心板与**所有**现存/未来扩展板都受其约束。因此本章遵循 [[09-风险与边界]] 决策优先级，把"接口稳定性/安全/可校准"放在功能丰富度之前，并对所有针脚分配预留冗余。

输入依赖：
- [[11-FPGA选型与IO预算]]：IO Bank 分配、pin 预算、连接器型号候选。
- [[10-Pattern引擎时序规格]]：skew 预算、等长约束、端到端时序口径。
- [[14-配置文件Schema规范]]：`io_mapping` / `board_id` / 安全字段的契约。

---

## 一、接口总览

沿用 [[03-核心FPGA主板设计]] 的 J1~J6 分区，给出每个连接器的承载与 pin 预算。

| 连接器 | 承载 | 信号 pin | GND/PWR pin | 合计估算 | 时序等级 |
|---|---|---:|---:|---:|---|
| J1 | DUT IO 组 A+B（64 路 + 组控制） | ~72 | ~72 | ~144 | 高 |
| J2 | DUT IO 组 C+D（64 路 + 组控制） | ~72 | ~72 | ~144 | 高 |
| J3 | DUT 扩展 IO 组 E+F + 协议引出（64~128） | ~80 | ~64 | ~144 | 中 |
| J4 | 电源与安全（辅助电源 + EN/PGOOD/FAULT/电流回读/急停） | ~24 | 大电流多 pin | 按电流定 | — |
| J5 | Trigger / Clock / Sync（含差分对） | ~16 | 配足 GND | ~32 | 高（敏感） |
| J6 | 调试 / 管理总线 / 预留 | ~16 | ~8 | ~24 | 低 |

> 设计取舍：**不把所有信号挤进一个大连接器**（机械应力、共面度、插拔力风险），按 Bank/用途分多个，且高速数字（J1/J2）与敏感时钟（J5）物理分离。

---

## 二、连接器选型与冻结建议

### 2.1 候选对比

| 类型 | 针距 | 单连接器容量 | SI（速率） | 机械可靠性 | 成本 | 适用 |
|---|---|---|---|---|---|---|
| **Samtec SEARAY (SEAF/SEAM)** | 1.27 mm | 高（可达 400+ 针，开放针场） | 好（≥ 数 Gbps） | 好（堆叠定型） | 中高 | **J1/J2/J3 主力推荐** |
| FMC LPC / HPC (VITA 57) | — | LPC 160 / HPC 400 | 好 | 标准化 | 中高 | 若需兼容现成 FPGA 生态 |
| Hirose FX 系列 | 0.8~1.27 mm | 中高 | 中好 | 好 | 中 | 备选 |
| 2.54 排针/排母 | 2.54 mm | 低 | 差（仅低速） | 一般 | 低 | **仅 J6 调试/预留** |

### 2.2 冻结建议

```text
J1/J2/J3：Samtec SEARAY 开放针场（如 SEAF-xx），按上节 pin 预算选行列数
          理由：高密度、堆叠高度可控、SI 余量足、地针可灵活穿插（满足 1:1 配地）
J4：      独立大电流连接器（功率针 + 信号针分离），不与高速数字混用
J5：      可用 SEARAY 子区或独立连接器，时钟/Trigger 走差分对 + 双侧 GND 屏蔽
J6：      可用低成本 2.54 排针（调试，不上高速）
```

> 待冻结（见第九节）：具体行列数、堆叠高度（建议 10~16 mm，留扩展板器件高度）、定位销规格。

---

## 三、数字 IO 针脚分配（J1/J2/J3）

### 3.1 通道捆绑（repeating unit）

为保证 SI 与可制造性，数字 IO 按**固定重复单元**布针，而非随意排列。高速组（J1/J2）采用 **S-S-G 模式（2 信号 1 地）**，敏感子集可加密到 1:1。

| 重复单元（每 8 针） | 内容 |
|---|---|
| 针 1 | 信号 ch_n |
| 针 2 | 信号 ch_n+1 |
| 针 3 | GND |
| 针 4 | 信号 ch_n+2 |
| 针 5 | 信号 ch_n+3 |
| 针 6 | GND |
| 针 7 | 信号 ch_n+4 |
| 针 8 | GND |

> 每 32 路 IO 组 = 4 个重复单元 + 组级控制/电压检测针（见 3.3）。GND 比例最终随 [[10-Pattern引擎时序规格]] 端到端 skew 实测调整。

### 3.2 单通道信号定义

每路 DUT IO 在 FPGA 侧具备（见 [[03-核心FPGA主板设计]] IO 能力）：输入/输出/三态、Pattern 驱动、Capture 采样、IDELAY 去偏斜。**方向控制是接口设计的关键难点**，见 3.3。

### 3.3 ⚠️ 方向控制（OE/DIR）——接口最易踩坑处

DUT IO 是双向的，扩展板上要有电平转换/缓冲。方向怎么控制，直接决定接口针脚数和时序能力，必须显式定义：

| 方案 | 针脚开销 | 时序 | 适用 |
|---|---|---|---|
| **自动方向（auto-dir）电平转换器** | 0（器件自判方向） | 差（带宽几十~几百 Mbps，有死区/glitch） | 低速组（J3 部分） |
| **OE 受控缓冲器（per-group）** | 每组 1~few 根 DIR/OE | 中（组内统一方向） | 多数 DUT IO |
| **OE 受控（per-pin）** | 每针 1 根 DIR | 好（任意脚任意方向） | 高时序要求子集 |

接口规范决策：
```text
· J1/J2 高速组：预留 per-group OE/DIR 控制总线（每 32 路组 4 根方向控制 + 1 根组 OE）
· 高时序子集（如每组前 8 路）：预留 per-pin DIR，支持任意脚任意拍方向切换
· J3 低速组：允许 auto-dir，省针脚
· 组级 Bank 电压检测针：每组 1 根，回读扩展板该组实际 IO 电压（安全校验 C5，见 [[14-配置文件Schema规范]]）
```

> 这是 [[10-Pattern引擎时序规格]] 4.4「端到端有效边沿 = min(FPGA侧, 板路侧)」在接口层的落地：**要精确边沿/异常注入的通道，必须走 OE 受控高带宽缓冲并占用 DIR 针脚**；省针脚的 auto-dir 组则明确标注为低时序档。扩展板配置文件须声明每组的时序等级。

---

## 四、电源与安全针脚（J4）

### 4.1 电源针

承接 [[04-扩展板与适配体系]]：核心板只供**受控辅助电源**，DUT 具体电压由扩展板生成。

| 信号 | 方向 | 说明 |
|---|---|---|
| 12V_RAW（或 12V_SW） | 核心板→扩展板 | 主供电，功率针多 pin 并联 |
| 5V_AUX | 核心板→扩展板 | 辅助 |
| 3.3V_AUX | 核心板→扩展板 | 辅助/逻辑 |
| PWR_GND | — | 功率地，与信号地单点/多点策略见第七节 |

### 4.2 安全与管理针（电气定义，不只功能）

| 信号 | 电气 | 方向 | 行为 |
|---|---|---|---|
| EXT_EN | 推挽，默认低 | 核心板→扩展板 | 高=允许扩展板辅助电源；默认低=不上电 |
| PGOOD | 开漏，上拉在核心板 | 扩展板→核心板 | 扩展板电源就绪拉高（释放） |
| **FAULT_N** | **开漏 wired-OR，上拉在核心板** | 扩展板→核心板 | **任一故障源拉低 → 管理 MCU 硬件关断** |
| ESTOP_N | 开漏 wired-OR | 双向 | 急停，硬件级，独立于上位机 |
| IMON | 模拟/数字回读 | 扩展板→核心板 | DUT 电流监控（见 4.3 Kelvin） |

> 关键：**FAULT_N / ESTOP_N 用开漏 wired-OR**，任何一块扩展板或核心板都能单方面拉低触发关断，**不依赖主 FPGA、不依赖上位机**——这是 [[02-总体系统架构]]/[[09-风险与边界]] 安全链路要求的硬件兜底在针脚级的体现。

### 4.3 ⚠️ 电流/电压回读的 Kelvin（4 线）问题——原方案缺失

[[04-扩展板与适配体系]]/[[05-MCU模拟外设测试方案]] 要求 DUT 电流监控、LDO 负载调整率测试，但**没提 force/sense 分离**。在连接器+排线压降下，2 线测量误差会很大。

```text
建议：对需要电流/电压精度的 DUT 电源轨，接口预留 Sense+ / Sense- 回检针（Kelvin 4 线）
      研发级功能验证可先 2 线；但接口针脚应预留 Sense 对，避免后续改接口
```

---

## 五、时钟 / Trigger / 同步针脚（J5）

| 信号 | 形式 | 说明 |
|---|---|---|
| CLK_OUT | 差分对（推荐）/单端 | 核心板→DUT/扩展板参考时钟 |
| CLK_IN | 差分对/单端 | 外部/DUT 回传时钟采样 |
| TRIG_IN | 单端 + 双侧 GND | 外部仪器触发平台 |
| TRIG_OUT | 单端 + 双侧 GND | 平台触发示波器/逻辑分析仪 |
| SYNC | 差分对 | 多板同步预留 |

> J5 信号对 skew/抖动敏感（见 [[10-Pattern引擎时序规格]] skew 预算）：差分对内等长、双侧 GND 屏蔽、与高速数字组（J1/J2）物理隔离布线。

---

## 六、板卡识别与管理总线（J6）

承接 [[04-扩展板与适配体系]]/[[14-配置文件Schema规范]]：

| 信号 | 说明 |
|---|---|
| MGMT_SCL / MGMT_SDA | 管理 I2C，读扩展板 EEPROM（board_id/版本/能力/校准数据） |
| GA0 / GA1 / GA2 | 地址选通针（多扩展板/堆叠时 EEPROM 地址 strap） |
| JTAG（TCK/TMS/TDI/TDO） | 调试 |
| UART（TX/RX） | 调试 |
| INT_N | 扩展板事件中断（开漏） |

> 上电时序（[[02-总体系统架构]] 控制流）：先读 EEPROM `board_id` → 与 [[14-配置文件Schema规范]] C1 交叉校验 → 通过才允许 EXT_EN 拉高 → 再 DUT 上电。

---

## 七、机械与接地

### 7.1 机械防呆（原方案只有"必须考虑"，本节给方案）

```text
· 连接器键位（Key）：J1~J6 用不同键位/缺针方向，物理上无法插错连接器
· 定位销：核心板 ≥2 个非对称定位销孔，防 180° 反插
· 堆叠：金属/尼龙立柱 + 螺丝固定，堆叠高度统一（建议 10~16 mm）
· 防误上电：未检测到合法 board_id（EEPROM）→ EXT_EN 保持低（见第六节）
```

### 7.2 接地架构（原方案列为"关键工程问题"但无架构）

```text
· 数字地（DGND）：J1/J2/J3 重复单元中 1:1~2:1 配地，提供高速回流
· 功率地（PGND）：J4 功率针就近回流
· 模拟地（AGND）：模拟扩展板上 AGND 与 DGND 单点星形连接，
                  跨板模拟信号优先走差分或带保护的采集路径（见 [[05-MCU模拟外设测试方案]]）
· 跨板（核心板/母板/子板）三级结构的模拟返回路径是真实风险，
  模拟前端尽量在母板就近 ADC 数字化，减少长模拟链路跨连接器
```

### 7.3 热插拔策略（原方案列风险但无策略）

```text
默认策略：断电插拔（power-off insertion）
  · 插拔扩展板前，上位机/管理 MCU 确保 EXT_EN=低、DUT 断电
  · 不支持带电热插拔扩展板（V1）
  · 若未来需热插拔，需专门设计预充电针、长短针时序——明确推迟 V2+
```

---

## 八、接口版本管理

接口是最高代价的契约，必须从第一版做版本号。

```text
· 接口修订号 interface_rev 存于扩展板 EEPROM 与核心板固件
· 核心板声明"支持的 interface_rev 范围"
· 上电时校验：扩展板 interface_rev 不在核心板支持范围 → 拒绝上电并告警
· 针脚定义只增不改：预留针(RSV)转正走兼容流程，已定义针不改语义
· 任何 pinout 变更 = 接口大版本号 +1，需评估对所有现存扩展板的影响
```

> 与 [[14-配置文件Schema规范]] 的 `schema_version` 协同：硬件接口版本（针脚）与软件配置版本（字段）分别管理、各自演进。

---

## 九、待冻结项

| 项 | 冻结条件 |
|---|---|
| 连接器最终型号/行列数/堆叠高度 | 完成完整信号清单 + 询价 + SI 仿真 |
| 每组 GND 比例（2:1 vs 1:1） | [[10-Pattern引擎时序规格]] 端到端 skew 实测 |
| per-pin DIR 覆盖哪些通道 | 结合首批 DUT 的高时序需求 |
| Kelvin Sense 对覆盖哪些电源轨 | 结合首批 DUT 电流精度需求 |
| 定位销/键位具体规格 | 机械结构设计阶段 |
| 功率针数量 | 按最大 DUT + 扩展板供电电流核算 |

---

## 当前结论

```text
· J1~J6 分区已落到逐针重复单元 + 信号清单 + 电气定义
· 连接器主力建议 Samtec SEARAY（J1/J2/J3），J4 独立功率连接器，J6 低成本调试
· 三个原方案缺失项已补：方向控制(OE/DIR)针脚策略、电流回读 Kelvin 预留、接地/热插拔/防呆方案
· FAULT_N/ESTOP_N 开漏 wired-OR，安全关断不依赖 FPGA/上位机
· 接口版本独立管理，针脚只增不改
```

本章把 [[10]][[11]][[14]] 的约束落到物理针脚，是进入核心板/扩展板 PCB 设计前的接口冻结基础。**建议与 [[11-FPGA选型与IO预算]] 同期冻结**（连接器选型与 FPGA 封装/Bank 强耦合）。
