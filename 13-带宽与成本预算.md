# 带宽与成本预算

## 章节定位

本章是 [[10-Pattern引擎时序规格]]、[[11-FPGA选型与IO预算]]、[[12-Capture引擎时序规格]] 三份规格共同指向的下游收口文档，用来验证平台的两个核心卖点是否成立：

1. **"实测墙"扛不扛得住** —— 数据链路（采样 → DDR → 上行回读）的带宽是否匹配 Capture 需求。
2. **"比 ATE 低成本"成不成立** —— 给出单板 BOM 成本区间锚点，让"低成本"从口号变成可核算的数字。

[[09-风险与边界]] 多处定性提到"回读可能成瓶颈""成本失控风险"，但全程没有任何量化计算。本章把这些坐实成数字与预算表。

> 纪律不变（[[09-风险与边界]]）：`安全性 > 架构复用性 > 可验证闭环 > 扩展余量 > 单点功能丰富度 > 成本极限优化`。本章所有数字为**目标/估算值**，须在 [[08-MVP实施计划]] 阶段 0/1 结合选型询价与 bring-up 实测冻结。

---

## 一、带宽预算：数据链路的三道关口

数据从 DUT 引脚到上位机磁盘，要穿过三道带宽关口，**最窄的一道决定平台实测能力**：

```text
DUT 响应
  ↓ ① 采样写入：Capture 引擎 → DDR 写入带宽
  ↓ ② 缓存吞吐：DDR 读写总带宽（同时承载 Pattern 回放 + Capture 写 + 回读）
  ↓ ③ 上行回读：DDR → 上位机（USB3 / GigE）   ← 通常最窄
上位机
```

### 1.1 原始采样数据率（输入侧）

按 [[12-Capture引擎时序规格]] 的目标参数计算波形记录模式的原始数据率：

```text
原始数据率 = 通道数 × 采样率 × 1 bit/采样/通道
```

| 场景 | 通道数 | 采样率 Fsamp | 原始数据率 |
|---|---:|---:|---:|
| MVP 典型 | 160 | 200 MSa/s | 160 × 200M × 1bit = **4.0 GB/s** |
| MVP 满配 | 192 | 200 MSa/s | **4.8 GB/s** |
| 预留满载 | 256 | 250 MSa/s | **8.0 GB/s** |

> 仅波形记录就是 **GB/s 量级**。这还没算 Pattern 回放占用的 DDR 带宽。

### 1.2 三道关口对比

| 关口 | 典型能力 | 与 4.0 GB/s 原始率对比 |
|---|---|---|
| ① DDR 写入（DDR3 x16 @800MHz） | ~3.2 GB/s 理论，实际有效 ~50-70% → ~1.6-2.2 GB/s | ❌ 已不够全速全通道连续写 |
| ② DDR 总带宽（需同时 Pattern 读 + Capture 写 + 回读） | 同上，且三方争用 | ❌ 更紧张 |
| ③ 上行回读 GigE | 实际有效 **~100 MB/s** | ❌ 差 **40 倍** |
| ③ 上行回读 USB3（5Gbps，实际） | ~200-350 MB/s | ❌ 仍差 **10+ 倍** |

### 1.3 关键结论：必须靠"按需 Capture"匹配带宽

全通道全速连续 Capture **在 V1 物理上不可行**——这不是缺陷，而是任何非 ATE 平台的固有约束。V1 通过以下机制把"瞬时高数据率"压缩到"可回读的数据量"：

```text
① 触发窗口（Pre/Post）  —— 只抓失败前后的有限窗口，不连续全量
② 通道掩码             —— 只记录关心的通道子集
③ 降采样 / 按需采样率   —— 观测用途用低采样率
④ DDR 作为弹性缓冲     —— 瞬时高速写 DDR，低速回读（解耦 ①②③ 与回读）
⑤ overflow_flag       —— 一旦超限如实标注丢点（见 [[12-Capture引擎时序规格]]）
```

> 设计要求（写入 [[12-Capture引擎时序规格]] 与 [[07-上位机软件设计]]）：
> - DDR 充当"瞬时高速写入 + 低速回读"的弹性缓冲，把 ① 和 ③ 的带宽鸿沟解耦。
> - 单次 Capture 窗口的**总数据量**必须可在合理时间内回读完。
> - 上位机配置 Capture 时，应**实时估算并显示"本次窗口数据量 / 预计回读时间"**，超出预算时告警，而非运行后才发现卡死。

### 1.4 单次 Capture 窗口数据量示例

```text
窗口数据量 = 通道数 × 窗口采样点数 × 1 bit
```

| 配置 | 通道 | 窗口深度 | 数据量 | GigE 回读时间 |
|---|---:|---:|---:|---:|
| 浅窗口（BRAM 模式） | 64 | 16 K 点 | 128 KB | ~1.3 ms |
| 中窗口（DDR 模式） | 128 | 1 M 点 | 16 MB | ~160 ms |
| 深窗口（DDR 模式） | 256 | 16 M 点 | 512 MB | ~5.1 s |

> 结论：**深窗口回读是秒级的**。这决定了 Capture 是"事件驱动抓窗口"而非"示波器式连续流"。V1 文档与 GUI 必须把这一点对用户讲清楚，避免被误解成"全程逻辑分析仪"。

---

## 二、上行通信选型结论（USB3 vs GigE）

[[03-核心FPGA主板设计]] 把 USB3 / GigE 列为二选一但未决。结合本章带宽分析给出**倾向性结论与决策条件**：

| 维度 | USB3 | GigE |
|---|---|---|
| 实际有效带宽 | ~200-350 MB/s | ~100 MB/s |
| 回读深窗口速度 | 快 2-3 倍 | 较慢 |
| 距离/组网 | 短（线缆） | 远、易组网、多机 |
| FPGA 侧复杂度 | 需 FX3 类桥接 | RGMII/SGMII + 协议栈 |
| 适合场景 | 单机调试、回读量大 | 远程/产线/多站、回读量适中 |

> 倾向性建议：
> - **MVP 主链路优先 USB3**（回读带宽是 Capture 的主要瓶颈，USB3 缓解最直接）。
> - **GigE 作为可选/并行链路**保留，服务于客户现场、产线、多站场景（呼应 [[01-需求与产品定位]] 的客户现场复现）。
> - 二者均保留 UART/JTAG 作调试链路（[[03-核心FPGA主板设计]]）。
> - 决策条件：若首要场景是"实验室单机 + 大量波形回读"→ USB3；若是"产线/远程多站"→ GigE。可在核心板预留两者 footprint，首版只贴一种。

---

## 三、DDR 容量与带宽预算

承接 [[11-FPGA选型与IO预算]] 待冻结的"DDR 容量/位宽/速率"。

### 3.1 容量需求分解

按 [[03-核心FPGA主板设计]] 的 Buffer 模型估算：

| Buffer | 用途 | 容量需求估算 |
|---|---|---|
| pattern_buffer | 长向量缓存 | 视最长 Pattern，数十~数百 MB |
| capture_buffer | 波形采样区 | 深窗口 256ch×16M ≈ 512 MB（见 1.4） |
| fail_window_buffer | 失败窗口区 | 数十 MB |
| log_buffer | 事件日志 | 数 MB |

```text
保守起步容量：512 MB ~ 1 GB
  → 与 [[07-上位机软件设计]] core_board.yaml 示例的 512MB 一致（但应标注为"示例/起步值"，非冻结）
  → 深 Capture 场景建议 1 GB
```

> 一致性修订提示：[[07-上位机软件设计]] 的 `core_board.yaml` 写死 `size_mb: 512`，而 [[03-核心FPGA主板设计]] 标 TBD。建议统一口径——**512MB 为起步示例值，深 Capture 目标 1GB，最终随本章预算冻结**。

### 3.2 带宽需求

```text
DDR 须同时承载（最坏情况）：
  Pattern 回放读 + Capture 写 + 上位机回读
理论需求峰值可达数 GB/s（见 1.1），但通过"非同时全速"调度缓解：
  · Pattern 回放与深 Capture 通常不在同一时刻全速
  · 回读在 Capture 停止后进行
```

> 设计建议：V1 不追求三方同时全速，**通过固件调度错峰**（Pattern 跑 → 停 → 回读）。真正需要"边跑边回读"的流式场景推迟 V2+。这与 [[09-风险与边界]]"先实现基础带宽闭环，再优化性能"一致。

---

## 四、单板 BOM 成本预算（"低成本"卖点的锚点）

[[01-需求与产品定位]] 把"比 ATE 成本低、导入轻"作为核心卖点，但全套文档**无任何成本数字**。本章给出**量级锚点**（非报价，用于选型取舍与定位校准）。

### 4.1 核心板主要成本构成（量级估算）

| 项 | 量级占比 | 成本驱动 | 备注 |
|---|---|---|---|
| FPGA（中端大封装，如 Artix-7 200T 类） | 高 | 单颗占比最大 | 见 [[11-FPGA选型与IO预算]] |
| 高密连接器（J1~J6 多个） | 中高 | 数量 + 密度 | 高密板对板单价不低 |
| DDR | 中 | 容量/速率 | |
| 多路程控电源 + 电源管理 | 中 | 路数 + 程控精度 | |
| 管理 MCU/CPLD + 保护器件 | 低中 | | 安全链路 |
| PCB（12~16 层 + 大 BGA 扇出） | 中高 | 层数 + 工艺 | 大 BGA 推高层数 |
| 上行通信 PHY（FX3 / GigE PHY） | 低中 | | |

### 4.2 成本定位结论

```text
核心板成本量级：明显高于普通 FPGA 开发板，远低于专业 ATE
  · vs 普通开发板：贵在 IO 规模、连接器、程控电源、安全链路、PCB 层数
  · vs 专业 ATE：省在不做 ps 级 timing ASIC、不做高并行、不做量产吞吐
定位校准：本平台的"低成本"是相对 ATE 而言的"研发级低成本"，
          不是"开发板级低成本"。文档对外宣传必须用这个口径，避免预期错位。
```

> 这与 [[09-风险与边界]]"产品定位被误解风险"直接相关：既不能被当成普通开发板（低估价值），也不能被当成 ATE 平替（高估能力/低估成本）。

### 4.3 成本-能力取舍杠杆

当成本超预算时，按以下顺序砍（遵循决策优先级，**安全与架构复用不可砍**）：

```text
可砍：① 预留 IO 数量（256→224）  ② 高速通道占比  ③ DDR 容量（1GB→512MB）
       ④ 程控电源路数            ⑤ 第二上行链路（只留 USB3）
不可砍：安全链路、扩展板识别、Pattern/Capture 基础能力、deskew 校准
```

---

## 五、带宽-成本联动的选型建议

把本章预算反馈给 [[11-FPGA选型与IO预算]]：

| 决策 | 建议 | 依据 |
|---|---|---|
| 上行链路 | MVP 主选 USB3 | 回读是主瓶颈（第二节） |
| DDR | 起步 512MB，深 Capture 目标 1GB | 容量预算（3.1） |
| DDR 位宽/速率 | DDR3 x16 起步够用，不必上 DDR4 | 错峰调度下带宽够（3.2） |
| Capture 策略 | 事件驱动抓窗口，非连续流 | 带宽墙（1.3） |
| 高速通道占比 | 控制差分/高速通道数量 | IO 与成本双约束 |

---

## 六、待实测/询价后冻结项

| 项 | 冻结条件 |
|---|---|
| DDR 实际有效带宽 | 选型 + bring-up 实测（MIG 效率） |
| USB3/GigE 实际回读速率 | 桥接方案 + 驱动实测 |
| 各 Buffer 容量分配 | 结合首批真实测试用例数据量 |
| 单板 BOM 实际成本 | 完成 BOM + 询价（[[08-MVP实施计划]] 阶段 1 交付物） |
| Capture 窗口数据量上限策略 | 上位机预算告警阈值需实测标定 |

---

## 当前结论

```text
带宽：
  · 全通道全速连续 Capture 物理上不可行（原始率 GB/s，回读 ~100MB/s~350MB/s）
  · 这不是缺陷，是非 ATE 平台的固有约束
  · V1 靠 触发窗口 + 掩码 + 降采样 + DDR 弹性缓冲 + overflow 标注 匹配带宽
  · Capture 是"事件驱动抓窗口"，不是"示波器连续流"——必须对用户讲清
  · 上行主链路建议 USB3，GigE 作产线/远程可选链路

成本：
  · 核心板成本量级：高于普通开发板，远低于专业 ATE
  · "低成本"是相对 ATE 的"研发级低成本"，非"开发板级"——对外口径必须校准
  · 超预算时砍预留余量/链路，绝不砍安全与架构复用
```

至此，"时序规格 → 选型 → 数据采集 → 带宽/成本"的工程规格链完整闭环：[[10-Pattern引擎时序规格]] 与 [[12-Capture引擎时序规格]] 定义能力，[[11-FPGA选型与IO预算]] 据此选型，本章用带宽与成本验证两个核心卖点并反向校准选型。下一步建议转向 P1 缺口：**四层配置文件 Schema 统一** 与 **寄存器映射文档**，把硬件规格链落到软硬件接口契约上。
