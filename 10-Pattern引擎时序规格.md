# Pattern 引擎时序规格（V1）

## 章节定位

本章为核心 FPGA 主板上 **Pattern 引擎的第一版（V1）时序规格**，目的是把 [[01-需求与产品定位]] 中列为核心卖点的能力——

> 精确边沿控制、多 IO 同步 Pattern、ns/us 级时序异常注入、失败前后窗口 Capture

——从定性描述落实为**可冻结、可选型、可验收的量化指标**。

[[03-核心FPGA主板设计]] 已经定义了 Pattern 引擎的功能轮廓（并行向量、循环、Trigger、比较），但**全程没有时序数字**。本章补齐这一缺口，因为：

> "能并行翻转 IO" 只是入门；**"能在确定时刻精确放置每根脚的边沿、并在指定时刻采样"** 才是本平台区别于一块普通 FPGA 开发板的根本。卖点和最含糊的模块原本是同一个，本章就是来消除这个矛盾的。

本章遵循 [[09-风险与边界]] 的决策优先级：

```text
安全性 > 架构复用性 > 可验证闭环 > 扩展余量 > 单点功能丰富度 > 成本极限优化
```

因此 V1 时序指标的取舍原则是：**先保证全通道可靠、可校准、可验收的中等时序，再谈高速**。不追求 ATE 级 ps 边沿，但要明确说清能做到哪一档。

---

## 一、为什么 Pattern 引擎的时序必须先冻结

它是一条贯穿全局的关键路径，下游决策全都依赖它：

```text
Pattern 时序规格（本章）
  ├─→ 决定 FPGA 选型（是否需要 SERDES、IDELAY/ODELAY、IO 速率等级）  → 见 [[03-核心FPGA主板设计]]
  ├─→ 决定 IO Buffer / 电平转换器件选型（带宽是否成为瓶颈）         → 见 [[04-扩展板与适配体系]]
  ├─→ 决定连接器、PCB 层数与等长约束（skew 预算）
  ├─→ 决定 Capture 引擎采样率与对齐方式                          → 见 [[03-核心FPGA主板设计]]
  └─→ 决定上位机 Pattern 数据格式与 time-set 抽象                → 见 [[07-上位机软件设计]]
```

> 结论：本章指标不冻结，FPGA 选型与 IO 通道方案就无从谈起。建议本章随 [[08-MVP实施计划]] 的"阶段 0 需求冻结"一起锁定。

---

## 二、术语与时序模型

为避免后续沟通歧义，先统一术语（参考 ATE 行业惯例，但按 FPGA 平台简化）。

| 术语 | 含义 |
|---|---|
| **Tester Cycle / 节拍 (Tcyc)** | 一个 Pattern 向量占用的时间，所有通道共享同一节拍 |
| **Time-Set** | 一组边沿时刻定义（drive 边沿、compare strobe 的相对位置），可被多个向量复用 |
| **Fine Grid / 细分栅格 (Tfine)** | 节拍内边沿可放置的最小时间步进 |
| **Drive Edge** | 激励边沿（输出从一个电平翻转到另一个电平的时刻） |
| **Format / 格式** | 激励波形形态：NRZ / RZ / R1 / SBC 等 |
| **Compare Strobe / 采样选通** | 读取 DUT 输出并与期望值比较的时刻 |
| **Edge Compare** | 在某个时刻点采样一次（点比较） |
| **Window Compare** | 要求电平在一段窗口内保持稳定（窗口比较） |
| **Skew / 通道偏斜** | 名义上同时刻的边沿，在不同通道实际到达 DUT 引脚的时间差 |
| **Deskew / 去偏斜** | 通过每通道可编程延迟，把各通道边沿对齐 |

### 单节拍时序模型

```text
        |<--------------------- Tcyc（节拍）--------------------->|
        |                                                          |
 Drive  |    T1R          T1F                                      |
 边沿   |-----^------------v--------------------------------------|----
        |     ___________                                          |
 波形   |____/           \_________________________________________|   (RZ 格式示例：两个边沿)
        |                                                          |
Compare |                              STB                         |
 选通   |------------------------------|---------------------------|----
        |                          采样此刻 DUT 输出并比较          |
        0                                                        Tcyc
```

- `T1R / T1F`：drive 边沿时刻，相对节拍起点，放置在 `Tfine` 栅格上。
- `STB`：compare 选通时刻，同样在 `Tfine` 栅格上。
- 一组 `{T1R, T1F, STB, format}` 即一个 **Time-Set**，向量只引用 Time-Set 编号，节省下发带宽。

---

## 三、核心架构决策（本章最关键的一节）

ATE 用专用 timing ASIC（formatter）实现每引脚 ps 级边沿。FPGA 平台无法直接对标，必须**老实回答：能做到哪一档、用什么机制做到**。V1 采用**三层时序能力分级**，按需启用，复杂度与精度逐层递进。

### 模式 A：节拍同步模式（必做，所有通道）

所有边沿对齐到节拍时钟沿，节拍内不放置边沿。

- 分辨率 = 节拍周期 `Tcyc`。
- 实现最简单，纯 fabric 逻辑即可。
- 适用：功能向量、协议正常时序、Reset/Boot 组合、批量回归向量。
- skew 来源：时钟树 skew + IO + 板级走线（见第五节）。

### 模式 B：SERDES 细分模式（V1 核心卖点，重点通道）

用 **OSERDES 把每个 fabric 节拍串行化为 N 个子位（sub-bit）**，从而在节拍内得到一个 `Tfine = UI`（线速率位周期）的细分栅格；drive 边沿可放置在任意子位边界，compare 用 **ISERDES 过采样**后在指定子位索引处取样。

```text
fabric 时钟 (例 125 MHz, 8 ns)
        │  OSERDES 8:1, 线速率 1.0 Gbps
        ▼
子位栅格: | b0 | b1 | b2 | b3 | b4 | b5 | b6 | b7 |   每格 = 1 UI = 1.0 ns = Tfine
          ↑__边沿可放在任意子位边界__↑
```

- 这天然支持 NRZ / RZ / R1 等**多边沿格式**——它们只是节拍内子位图样的不同编排，不需要额外的边沿发生器。
- 适用：精确边沿控制、ns 级时序异常注入（毛刺、边沿微移、半周期破坏）、高并行同步激励。

### 模式 C：延迟线去偏斜（V1 仅用于静态校准，不做逐向量动态边沿）

用每通道 **ODELAY/IDELAY 抽头**补偿"FPGA 封装 + PCB 走线 + 连接器 + 电平转换器"引入的**静态通道偏斜**，把各通道边沿对齐。

- V1 定位：**静态 deskew**（开机校准一次，写入校准表），不做逐向量的动态 per-pin edge placement（那是 V2+ 才考虑的真 formatter 能力）。
- 抽头步进取决于 FPGA（典型 ~78–150 ps/tap，详见选型后实测）。

> 决策小结：
> - **V1 必做 模式 A + 模式 C**（同步向量 + 静态去偏斜），这是"可靠、可校准、可验收"的底线。
> - **V1 核心增量做 模式 B**（SERDES 细分），把"精确边沿/异常注入"这个卖点真正兑现，但只在重点通道全速启用。
> - **真·每引脚动态边沿定时（per-vector per-pin formatter）明确推迟到 V2+**，避免 MVP 失控。

---

## 四、V1 时序指标（目标值）

> ⚠️ 以下为**目标值（target）**，最终值取决于 FPGA 选型与打样后的实测表征（bring-up characterization）。凡涉及 UI、抽头、skew 的绝对数字，均须在 [[08-MVP实施计划]] 阶段 3 bring-up 中实测冻结。本章先给出**自洽的一组目标**，作为选型锚点。

### 4.1 节拍与边沿

| 指标 | V1 目标 | 说明 |
|---|---|---|
| 节拍率（全通道保证） | ≤ 50 MHz（Tcyc ≥ 20 ns） | 模式 A/B 下所有 IO 都满足 |
| 节拍率（重点通道 stretch） | 100 MHz（Tcyc 10 ns） | 仅高速子集，依赖 FPGA Bank |
| 细分边沿栅格 Tfine（模式 B） | **1.0 ns**（8:1 OSERDES @ 1.0 Gbps） | 边沿放置的最小步进 |
| 每节拍 drive 边沿数 | ≥ 2（支持 NRZ / RZ / R1） | 由子位图样实现 |
| 每节拍 compare 选通 | 1（edge compare）；window compare 为 stretch | |
| 最小输出脉宽 | ≥ 2 UI ≈ 2 ns（FPGA 侧）| **端到端受电平转换器带宽限制，见 4.4** |
| 节拍数 / Pattern 深度 | 受 DDR 容量限制（见 [[03-核心FPGA主板设计]] DDR 章节） | 长向量走 DDR 回放 |

### 4.2 边沿放置分辨率与去偏斜

| 指标 | V1 目标 | 说明 |
|---|---|---|
| 边沿放置分辨率（模式 B 粗） | 1.0 ns（1 UI 栅格） | |
| 去偏斜抽头步进（模式 C） | ~78–150 ps / tap（FPGA 依赖） | IDELAY/ODELAY |
| 校准后通道间残余 skew | **< ±0.5 ns**（目标）| 校准后 FPGA 侧对齐目标 |
| 校准前通道间 skew | ±2–3 ns（典型，板路引入） | 见第五节预算 |

### 4.3 Compare / 采样

| 指标 | V1 目标 | 说明 |
|---|---|---|
| 采样方式 | edge compare（ISERDES 过采样取指定子位） | |
| 采样栅格 | 与 drive 共用 1.0 ns 细分栅格 | |
| window compare | stretch（V1 可降级为多点 edge compare 近似） | |
| 实时比较 | expected vs actual 逐通道掩码比较，记录首个失败地址 | 对应 [[03-核心FPGA主板设计]] Fail Capture |

### 4.4 ⚠️ 端到端真实边沿能力 = min(FPGA 侧, 板路侧)

这是**最容易被高估的一点，必须显式写清**：

- FPGA 侧（OSERDES + IDELAY）可以做到 1 ns 栅格、Gbps 级翻转。
- 但 DUT 看到的边沿要穿过 **扩展板的双向电平转换器**（见 [[04-扩展板与适配体系]]）。
  - **自动方向（auto-direction）电平转换器**（如常见 TXS/TXB 系列）实际带宽常只有几十 Mbps ~ 几百 Mbps，且有方向切换死区与 glitch 风险——它会成为**真实边沿速率的硬上限**。
  - **OE/DIR 受控缓冲器**带宽更高，但方向切换需要 Pattern 显式控制 DIR，引入额外时序开销。

> 结论：V1 必须区分两个口径并在报告中分别标注——
> - **"FPGA 端时序能力"**（1 ns 栅格、≥50 MHz 节拍）
> - **"DUT 端有效时序"**（受具体扩展板电平转换路径限制，须按扩展板实测标定）
>
> 这条直接影响 [[04-扩展板与适配体系]] 的器件选型：**追求精确边沿/异常注入的通道组，应选 OE 受控高带宽缓冲器，并在扩展板配置文件中声明其时序能力等级。**

---

## 五、端到端通道间 Skew 预算

名义"同时"的边沿，从 FPGA 到 DUT 引脚要经过多级，每级都贡献 skew。V1 必须建立预算表，否则去偏斜无的放矢。

| skew 来源 | 典型量级（未校准） | 处理方式 |
|---|---|---|
| FPGA 内部时钟树 + IO 路径 | 数百 ps | 模式 C 抽头校准 |
| FPGA 封装走线 | 数十~百 ps | 校准吸收 |
| 核心板 PCB 走线长度差 | 视等长约束，~数百 ps/inch | **PCB 等长约束**（见下） |
| 板间连接器 | 数十 ps | 校准吸收 |
| 扩展板 PCB 走线 | 数百 ps | 校准吸收 |
| **电平转换器传播延迟差** | **可达 ns 级，且随方向/温度变化** | 最大不确定项，需器件选型 + 校准 |
| DUT 子板走线 | 视设计 | 子板设计约束 |

设计约束（须写入 [[03-核心FPGA主板设计]] / [[04-扩展板与适配体系]] PCB 约束）：

- 同一 Pattern 组的数字 IO **走线等长**，组内长度差控制目标 < 对应 5 mm（约 < 30–35 ps）。
- 电平转换器**同组同型号同批次**，减少传播延迟离散。
- 关键通道预留 deskew 校准用的环回/测试点。

> 预算结论：未校准端到端 skew 现实在 ±2–3 ns 量级（电平转换器主导）；模式 C 静态校准后 FPGA→连接器段可压到 < ±0.5 ns，但**电平转换器之后的部分是校准盲区**，只能靠器件选型与等长约束控制。这是 V1 精度的真实天花板，须如实告知用户。

---

## 六、Pattern 数据结构与 Time-Set

### 6.1 向量格式（FPGA 可执行层）

沿用并细化 [[03-核心FPGA主板设计]] 的格式：

```text
向量字段:
  time_set_id        引用的 Time-Set（含 T1R/T1F/STB/format）
  io_value[N-1:0]    本节拍各通道目标值
  io_oe[N-1:0]       输出使能（0=高阻/输入，1=驱动）
  compare_mask[N-1:0]需要比较的通道
  expected[N-1:0]    期望采样值
  control_flags      Loop / Repeat / WaitTrigger / Stop / CaptureEn
```

关键点：**把"时序"从"数据"里抽出来**——向量只携带逻辑值与 Time-Set 编号，具体边沿时刻由 Time-Set 表统一定义。这样：

- 下发带宽大幅降低（时序不随每拍重复传输）。
- 上位机可像 ATE 一样以 time-set 思维编排测试，对接 [[07-上位机软件设计]] 的资源抽象。

### 6.2 Time-Set 表（上位机抽象示例）

```yaml
time_sets:
  ts_nrz_50m:           # 50 MHz NRZ，节拍起点驱动，节拍 75% 处采样
    tcyc_ns: 20.0
    format: nrz
    drive_edge_ns: 0.0
    strobe_ns: 15.0
  ts_rz_glitch:         # RZ 格式 + 注入一个 2ns 窄毛刺（异常注入示例）
    tcyc_ns: 20.0
    format: rz
    drive_on_ns: 2.0
    drive_off_ns: 4.0   # 仅 2 ns 高电平 → 窄脉冲/毛刺
    strobe_ns: 15.0
```

> 所有 `*_ns` 必须落在 1.0 ns 细分栅格上（模式 B）；非栅格值由编译器对齐并告警。

### 6.3 异常注入与时序的对应关系

[[06-流片前FPGA原型验证方案]] 与 [[01-需求与产品定位]] 列出的异常注入，落到本规格上即：

| 异常类型 | Pattern 时序实现 | 依赖模式 |
|---|---|---|
| 时钟毛刺 / 窄脉冲 | 子位级窄脉宽（≥2 UI） | 模式 B |
| 边沿微移 / 建立保持破坏 | strobe 或 drive 边沿移动 1 UI 步进 | 模式 B |
| 半包 / 截断 | 提前 Stop 或改写向量序列 | 模式 A |
| ACK/NACK / 位翻转 | 改写 expected / io_value | 模式 A |
| 时钟暂停 | WaitTrigger / 插入空节拍 | 模式 A |

---

## 七、与 Capture 引擎的对齐

Pattern drive 与 Capture compare 必须同源同步，否则采样点漂移会误判。

- Pattern 引擎与 Capture 引擎**共用同一 fine 时钟域与 Trigger**，由 [[03-核心FPGA主板设计]] 时钟系统统一分发。
- compare strobe 与 drive edge 在**同一 Time-Set 内定义**，保证相位关系确定、可复现。
- Fail Capture 记录的时间戳以 fine 栅格为基准，便于上位机回放对齐（对接 [[07-上位机软件设计]] Capture 管理）。

---

## 八、校准方法（模式 C 落地）

V1 校准定位为**静态、开机/换板时执行一次**，结果写入校准表（核心板与扩展板 EEPROM，见 [[04-扩展板与适配体系]]）。

```text
环回/已知基准
  ↓
逐通道扫描 ODELAY/IDELAY 抽头
  ↓
测定各通道边沿到达相对偏差
  ↓
求解每通道补偿抽头，使组内对齐
  ↓
写入 core_board 校准表 + 扩展板校准数据
  ↓
报告中标注：校准日期、残余 skew、未覆盖段（电平转换器之后）
```

报告须按 [[05-MCU模拟外设测试方案]] 同样的纪律，**区分"平台校准后残余误差"与"DUT 真实行为"**，避免把平台 skew 误判成 DUT 问题。

---

## 九、V1 边界（做 / 不做）

遵循全项目的边界纪律。

### V1 做

- 模式 A 同步向量（全通道，≤ 50 MHz）
- 模式 B SERDES 细分（重点通道，1 ns 栅格，NRZ/RZ/R1）
- 模式 C 静态去偏斜校准（残余 < ±0.5 ns 目标，FPGA→连接器段）
- Time-Set 抽象 + 向量/期望/掩码比较
- 基础异常注入（毛刺、边沿微移、半包、位翻转、时钟暂停）
- 与 Capture 同步 + Fail Capture 时间戳

### V1 不做（推迟 V2+）

- 真·每引脚逐向量动态边沿定时（per-pin formatter）
- < 100 ps 级边沿放置 / ps 级 deskew
- 完整 window compare（V1 用多点 edge 近似）
- 高速串行协议（SerDes/眼图）时序——明确不在范围，见 [[09-风险与边界]]
- 温度补偿动态再校准（V1 仅记录温度，不闭环）

---

## 十、对 FPGA 选型的硬性要求（输出给选型决策）

本章反推出 FPGA 选型的**时序相关必选项**，供 [[03-核心FPGA主板设计]] 选型时使用：

| 能力 | 要求 | 用途 |
|---|---|---|
| OSERDES / ISERDES | 必须，≥ 8:1，线速率 ≥ 1.0 Gbps | 模式 B 细分栅格 |
| IDELAY / ODELAY（可编程抽头） | 必须，抽头 ≤ 150 ps | 模式 C 去偏斜 |
| 足够 IO + Bank 电压灵活性 | 见 [[03-核心FPGA主板设计]]（160~192 用 / 256 预留） | 通道规模 |
| PLL/MMCM 数量 | 满足 fabric + fine + capture 多时钟域 | 时序生成 |
| BRAM/URAM | 满足无 DDR 时的短向量与 Capture | 缓存 |

> 若选用国产 FPGA 做替代方案，**OSERDES/IDELAY 等价能力与线速率是第一道筛选门槛**——没有这两项，模式 B/C 无法实现，平台就退化成"只有模式 A 的普通 FPGA 板"，核心卖点丢失。

---

## 十一、验收测试（对接 MVP 阶段 6）

为 [[08-MVP实施计划]] 的 Demo 闭环补充**时序专项验收项**：

| 验收项 | 方法 | 通过标准 |
|---|---|---|
| 节拍率 | 全通道 50 MHz NRZ 环回 | 无误码 |
| 细分边沿栅格 | 输出 1 UI 步进扫描，示波器/环回测量 | 边沿步进 ≈ 1 ns |
| 通道间 skew（校准前/后） | 多通道同时沿，测最大偏差 | 校准后 < ±0.5 ns（FPGA→连接器段） |
| 异常注入 | 注入 2 ns 毛刺并 Capture | 毛刺可复现、可观测 |
| 端到端有效边沿 | 经实际扩展板电平转换路径测量 | 标定并记录该扩展板时序等级 |
| Pattern/Capture 对齐 | 已知激励→采样比对 | 采样点相位稳定、可复现 |

---

## 当前结论

```text
V1 不追 ATE 级 ps 边沿，但要老实给出一档可靠数字：
  · 全通道 50 MHz 同步向量
  · 重点通道 1 ns 细分栅格 + 多边沿格式 + ns 级异常注入
  · 静态去偏斜，校准后 FPGA 段 < ±0.5 ns
  · 端到端有效时序 = min(FPGA 侧, 电平转换路径)，必须分口径标注
```

这份规格把"精确边沿控制"这个原本最含糊的卖点，变成了可选型、可校准、可验收的工程目标，并明确把真·formatter 能力推迟到 V2+。它同时给 FPGA 选型和扩展板电平转换器选型反向提出了硬性约束——**这两项应随阶段 0 一起冻结**。
